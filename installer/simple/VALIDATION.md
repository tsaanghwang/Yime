# 本轮验证记录（2026-09-14）

开发机 MYCOMPUTER 已使用当前简版维护入口完成两套卸载后重装。载荷沿用已验收 dev4 运行文件；本轮不声明全部原生二进制从清理后的源码重新构建。

- 所选两套先检查再逐套卸载，两套目录均移除后再逐套安装。
- 重装后全部载荷哈希匹配，两个产品的 x64/x86 COM 路径正确。
- Rime/PIME 原生注册验证通过；YimeCore 注册工具成功。提升权限的只读检查确认当前安装根中的 Launcher、Runtime 和 Broker 进程。
- 用户已确认两套输入法重启前后输入正常。这是用户实机确认，不是自动化宿主测试结果。

本地源码检查：Go stable 全量回归、固定 i686 Rust Launcher 单元及集成测试、Python 工具与 checked PowerShell 测试、CI 图与分片检查、构建契约与工具链清单、开发范围和平台源码检查、语流音变 63 项静态/纯测试及 21 项构包检查通过。修改后的 PowerShell 文件通过 PS5 语法检查。

简版隔离测试覆盖两套真实载荷、自有目录边界、私有字体占用、损坏安装重装和中断复制清理；通用选择覆盖单套/双套安装卸载以及失败停止；临时系统注册项覆盖启动项写入、读回和删除。

推送后仍需该提交 CI 成功；测试端维护矩阵尚未执行，不将开发机结果代替测试端最终验收。完整二进制包与源码提交分开交付，不能把 `.tmp` 中的本地文件误称为已发布的 GitHub 下载包。

## 首次测试端失败后的日志补正

测试端实际选择安装两套，Rime/PIME 失败后未执行 YimeCore；Restart Manager 报告 Explorer/Codex 加载 DLL、System 显示私有字体占用。缺少原提权子进程错误，未将退出码 1 等同于取消或占用。

安装脚本现将普通入口和提权子进程的日志保存在用户主目录 `Yime Setup Logs`，使用同一编号，包含异常详情、失败阶段和子进程退出码。取消操作和 UAC 取消单独编码。已在开发端用真实 UAC 执行可控的安装前错误，确认父子日志完整，无安装或注册修改。缺包失败、取消及嵌套 UAC 异常有隔离回归覆盖。

此修复提高错误可诊断性，不声称已经解决所有原生文件占用，也不据此将测试端维护矩阵标记通过。

带日志完整包 `simple-logging-dev6` 已在开发机实际执行两套卸载后重装；两套目录移除、安装文件哈希、x64/x86 COM 及后台映像路径检查通过。用户以“世代友好！世代友好！”确认两套候选和上屏正常。此轮新包尚未单独取得重装后的重启确认，不复用上一轮重启结果作本轮证据。本地日志位于 `.tmp/simple-logging-candidate-20260914-135119` 及用户主目录 `Yime Setup Logs`。
