# 简版安装器验证范围

更新：2026-09-16。本文汇总各次验证，不把源码/隔离检查、已交付包和实机输入合并为一个状态；原始报告与日志保留原始日期和字节。

## 最新源码修复（已合入，未交付新包）

PR #57 已合入 `main` 的 `76376080`，[合并后 CI](https://github.com/tsaanghwang/Yime/actions/runs/35041052421) 成功。

- `Test-PackageValidation.ps1`：用合成包验证构包和维护共用的 `Read-Package` 资源校验，拒绝缺少 YimeCore 图标、输入工具栏、练习器或工具中心的包。
- `Test-ProfileRemoval.ps1`：用户输入配置移除失败时，阻止后续 COM 注销、启动项清理和安装文件删除，覆盖两产品维护路径与清理顺序。
- 两组检查与启动项、单套/双套调度、日志、进程等待回归一起纳入 `simple-installer` CI，使用 Windows PowerShell 5.1 的隔离测试。

本节证明源码修复及相应自动回归通过，不证明新安装包已实机验收。包含修复的新完整包尚未交付；下列源码候选包和维护包结果保持各自身份，不自动升级为 PR #57 的安装结论。当前测试端没有新增任务，见 [HANDOFF](HANDOFF.md)。

## 清理后源码候选包验收（2026-09-14）

`a4fa6f76` 对应 CI 成功后，已验收完整源码候选包通过 GitHub Release 交付“计算机”。测试端两套安装日志均成功，用户确认在 Codex 与 Notepad（记事本）重启前后输入正常；Word 未报告结果，不计为通过。开发端已核对包身份及 10 个原始证据文件哈希，见 [本轮复核](../../docs/testing/simple-maintenance/2026-09-14/source-candidate-test/REVIEW.md)。本轮结束保留两套安装，不重复下述历史维护测试。开发机首次注册冲突仍保留在 [开发验收记录](../../docs/testing/simple-maintenance/2026-09-14/source-candidate/DEVELOPMENT.md)。

## 简版维护包：开发端（2026-09-14）

MYCOMPUTER 已完成两套卸载、重装、载荷哈希、x64/x86 注册及运行路径检查；用户确认两套重启前后输入正常。维护包运行载荷沿用已验收 dev4，不能宣称是全部清理后源码的重新构建。

普通入口提权后曾等待常驻子进程，`5c9a5d77` 改为只等待管理员安装进程。开发端真实 UAC 模拟与 `Test-ProcessWait.ps1` 覆盖成功/失败返回且后台子进程仍存活；`Test-Logging.ps1` 覆盖父子日志、取消和错误编码。简版测试另覆盖产品文件边界、私有字体占用、重装、系统启动项和单套/双套调度。

## 简版维护包：“计算机”测试端（2026-09-14）

`5c9a5d77` 对应 [CI 34815261435](https://github.com/tsaanghwang/Yime/actions/runs/34815261435) 成功后交付并测试；详见 [原始报告索引](../../docs/testing/simple-maintenance/2026-09-14/README.md)。

- 两套安装与外层正常返回：日志通过；两套重启前后切换、输入：用户确认。
- 分别卸载一套后另一套仍可输入，随后装回正常：两方向均有日志与用户确认。
- 一次选择卸载两套，以及各产品单独安装/卸载：均有成功日志。最终两套目录均不存在。
- 部分卸载尝试因文件无法独占读写而取消，退出 2；正常重启后不切换待维护输入法再操作成功。保留这一实测条件，不保证使用后始终无需重启即可维护。
- 未细分单装输入与具体应用覆盖，未独立核验最终输入法/应用条目 UI；不把这些项目补写为通过。

上述维护验证按已确认范围收尾。此后源码清理、文档更新不自动重启测试矩阵；新运行载荷的实机验收必须针对新包另行记录。开发与测试今后按 [分支交接](HANDOFF.md) 交换说明、报告和必要数据。
