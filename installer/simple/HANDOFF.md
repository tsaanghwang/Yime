# 简版维护验收交接（2026-09-14）

最新更新：普通用户提权等待修复已通过开发机回归及真实 UAC 检查。现在只等待管理员安装进程，不再等待其常驻后代。测试端已暂停旧入口；待本次提交 CI 成功，开发端交付完整修正版并更新共享目录当前交接入口，届时才继续执行。

本轮状态：开发机完成两套卸载及重装，用户确认重启前后均可正常输入。等待本轮提交的 CI 成功，测试端暂不启动新维护测试。

首次测试端失败后的更新：带完整提权日志的 `simple-logging-dev6` 已在开发机完成两套卸载、重装和实际输入确认。该修复正在提交与 CI 验证，测试端继续暂停旧包。日志在用户主目录 `Yime Setup Logs`，普通入口和提权进程按同一编号对应；最终失败以子进程日志为准。它不代表 Explorer/System 占用情形已经全部解决。

本地维护包目录为 `.tmp/simple-maintenance-20260914-121659`。`local-maintenance.log` 记录两套卸载后再安装，`installed-verification.txt` 记录两套 x64/x86 COM 和后台程序映像路径。安装后全部载荷哈希匹配。

该包使用本轮通用入口和已验收的 dev4 运行载荷，属于维护验证包，不宣称所有运行程序在本轮重新编译。Git 推送不会携带 `.tmp` 二进制文件。CI 成功后须交付完整包、校验值及对应提交，不能让测试端把拉取源码当成收到包。

测试端收到完整包后按顺序执行并记录实际输入：

1. 仅安装 YimeCore，验证输入，再卸载。
2. 仅安装 Rime/PIME，验证输入；随后安装 YimeCore，验证两套。
3. 卸载 YimeCore，确认 Rime/PIME 仍能输入；装回。
4. 卸载 Rime/PIME，确认 YimeCore 仍能输入；装回。
5. 一次选择卸载两套；一次选择安装两套，验证输入及重启后输入。
6. 最后一次选择卸载两套，确认两套条目和自有安装目录已移除。

数据默认保留，只有明确需要重置时使用 `-ResetData`。发现失败停止该步骤，回传错误和日志，由开发端处理；不让测试端改写安装器或接续旧恢复脚本。维护入口面向受支持的 Windows 机器，不绑定“计算机”或开发机。
