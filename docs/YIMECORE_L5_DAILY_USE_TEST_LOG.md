# YimeCore L5 日常使用测试日志

## 2026-09-04：本轮测试完成登记

- 用户在本会话明确反馈“这次测试已经完成”，按用户确认登记为本轮测试已完成；没有把这句话拆成六类场景全部 PASS，也没有推定两日观察已结束。
- 本会话最初请求中的 local.9 是旧版本名称。只读核对确认当前实际安装和运行的是 `0.1.0-local.11`，本日后续证据绑定 local.11；没有降级、重装或把 local.11 结果标为 local.9。
- 已有本机 x86 分支验收完成，证据见 [local.11 宿主验收](YIMECORE_LOCAL11_X86_ACCEPTANCE_2026-09-04.md)。该记录中的 x64/x86 三模式 6/6 和 Firefox/Notepad++ 人工三项结论已查阅原始 summary，本轮未重新执行，也不替代 x64 两日观察。
- 当前没有新增、可复现的产品回归报告。未测、等待物理操作及证据不足均不作为产品回归。
- `local_product_ready=false`、`public_release_ready=false`；L5 全阶段与 L6 仍未关闭。

## 两日窗口的依据及本日安排

上次会话“已重启”（task `01a06700-890c-71d3-96d5-67c102bdf70e`，turn `01a06a19-1432-7e62-91a0-78f9d47fc9c3`）提出“正常使用约两个工作日”，覆盖长文本、切换应用、失焦重入、设置和学习持久化、异常恢复。与 [实施计划](project/YIMECORE_LOCAL_PRODUCT_IMPLEMENTATION_PLAN.md) 第 2 节和 L5 一致。未找到已确认的每日细分记录，因此以下是本轮新制定的执行清单，不冒充上次已经完成的结果。

今天作为首个有结构化记录的观察日 D1；若用户补充已完成的前一工作日证据，再按实际日期并入，不能凭日历跨度补记 PASS。短时定向测试不能替代正常工作日观察。D2 为下一实际使用工作日，不要求周末加测，不自动重启、调度后台监控或更改默认输入法。

## D1 自动基线：已执行

- 采集时间和完整元数据：[2026-09-04-baseline.json](testing/l5/2026-09-04-baseline.json)。
- Manifest SHA-256：`5a3f847a3136fd2198f7dc9aba22dc017ed7439d8447435e8752eb379a4a5cd8`。
- 安装根：`C:\Program Files\YimeCore Experimental Trial\yimecore-e6c-f435f463bfd0-5a3f847a`。
- 74/74 清单文件完整性匹配，包括包内冻结载荷的只读检查；未运行冻结目标。
- Runtime PID `31460`、Broker PID `31488`；路径及启动时间与当前包、本次开机相符。这里只证明当前运行身份，不替代 Shell-Core 登录自启动证据。
- 当前 x64 COM 的系统视图指向本包 DLL。
- 通过 out-of-process `StdRegProv` 建立 12 项生产/冻结注册与默认语言设置指纹；不读取或改写生产用户数据。日终须再次比较，基线本身不证明整个观察窗口未变。
- 16:58 用户反馈“两个窗口已准备好”后，只读核对 Word PID `7984`（版本 `16.0.20326.20112`）已加载当前 local.11 的 `x64\YimeTextServiceExperiment.dll`，SHA-256 为 `a9f7ceaaa2d7e48ac6779269e94f1a286eecf67d45a0799630f7a5a67eb61900`。当次查询未发现 `notepad.exe` 或 `notepad++.exe`，第二个窗口所属应用待用户说明；不推定其不存在，也不作为产品故障。模块加载不代替当前 profile 的物理选择确认，D1-F 行为结果仍待测试。未读取标题或正文。
- 准备结束后再次只读比较：包身份及 12 项保护指纹未变化；脚本语法检查和错误 manifest 拒绝测试通过。见 [准备阶段复核](testing/l5/2026-09-04-preparation-verification.json)。该短时比较不是 D1 日终结论。

## D1 人工执行清单

17:05 更新：用户说明此前在 Codex 内打开夹具，现已改用 Notepad++。只读确认 Word PID `7984` 为 AMD64，Notepad++ PID `35848`（8.9.8.0）为 I386；分别加载当前 local.11 的 x64/x86 DLL，哈希与已验收包一致，见 [本日宿主就绪证据](testing/l5/2026-09-04-word-notepadpp-ready.json)。D1-F 实际配对调整为 Word x64 ↔ Notepad++ x86（本机 WOW64），不记为两个 x64 宿主。窗口准备完成不代表焦点测试通过；待物理确认 profile 并完成往返操作。这是夹具打开方式的澄清，不是产品回归。

合成夹具在仓库和 AppData 之外：`C:\Users\tsaan\YimeCore L5 Fixtures\2026-09-04`。它们不是恢复介质。只在这些专用文档或新的空白 Word 文档测试；不要覆盖已有文档。夹具隔离的是文档，不是已安装产品的设置/学习状态；正常选择会产生合成测试词的学习，设置测试只通过当前产品 UI 临时改变并最终还原，不重定向日常 Runtime 到测试数据目录。

| ID | 本日操作与可判定证据 | 当前状态 |
| --- | --- | --- |
| D1-F | 实际配对为 Notepad++ x86 专用夹具 ↔ Word x64 空白文档。两边物理选择当前“音元拼音”；按本会话指引在未提交组合时往返 5 次，最后各提交一短句，检查串窗提交、重复/丢字、候选残留、卡死/退出。模块证据见上方就绪记录；失焦时组合正常提交/取消本身不算错。 | PASS（用户人工确认，登记于 2026-09-04 17:46 +08:00） |
| D1-L | 按本会话指引将 B 的 120 段合成背景复制至 Word 专用文档，在开头、中间、结尾各手工输入至少两句，做退格、换行、撤销/重做；背景生成或复制不算输入验收。 | PASS（用户人工确认，登记于 2026-09-04 19:21 +08:00；实际用时未提供） |
| D1-P | Word 专用文档按本会话指引验证翻页与提交。用户明确确认单字和多字翻页正常、Shift+1～5 正常、裸数字作为编码键正常；Shift+6～9 未单独测试，不扩记覆盖。 | PASS（用户人工确认，登记于 2026-09-04 19:33 +08:00；覆盖范围如左） |
| D1-S | 在当前音元拼音设置 UI 中将候选数从 5 临时改为 6；用户确认设置重开及 Word 窗口重开后均保留，输入有效。只读核对 Word 仍为 PID `7984` 并加载当前 local.11 x64 DLL，故这是设置 UI/宿主窗口重入持久化，不声称 Word 进程已重启。跨 Runtime/电脑重启保留及恢复原值仍留 D2。 | PASS（用户人工确认，登记于 2026-09-04 19:43 +08:00；D1 范围） |
| D1-T | 用户明确提供页号/序号：初始 `(5/1)`，第 1 次选择后 `(1/6)`，第 2 次后 `(1/2)`，第 3、4 次后 `(1/1)`；另报告“遗忘后还原”。候选提位效果可辨认，未记录词或编码。该样本已遗忘，不能据此检查 D2 学习保留；须另留未遗忘的训练样本。 | PASS（学习提位的用户人工确认，2026-09-04 20:17 +08:00）；跨重启保留待测 |
| D1-R | 用户按本会话指引确认正常宿主重开后输入恢复、候选数仍为 6。20:31 只读核对 Word 已从 PID `7984` 换为 `36200`，且加载当前 local.11 x64 DLL。只计正常宿主重开恢复，不冒充异常故障恢复或 Runtime 重启。 | PASS（用户人工确认并有新宿主进程证据，2026-09-04 20:31 +08:00） |
| D1-E | 本轮定向测试收尾：74 个包文件匹配，12 项生产/冻结注册与默认语言设置指纹相对 D1 基线均未变；当前运行身份正确。D1-F/L/P/S/T/R 的限定范围人工检查均通过。用户补充本日累计约 2–3 小时间歇使用，暂无意料之外的异常；另报告分词有些不顺，待非敏感复现。 | D1 本日记录完成（用户估计时长，非连续整日使用）；机械检查 PASS，L5 总体验收仍待 D2/最终确认 |

D1-F 确认来源：用户在上述操作指引后回复“D1-F 通过，两边均选中音元拼音”。仅登记该场景通过，不推定其他场景或整日观察通过；未采集输入正文，未测量延迟，宿主 PID/模块关联沿用 17:05 就绪快照，不冒充确认时重新采集。

D1-L 确认来源：用户回复“D1-L 通过，约 X 分钟”。记录为 Word 长文本定向编辑场景人工通过；X 是未填写的时长，不转成数字，也不以会话间隔推算使用时间。未采集正文或测量延迟，不据此关闭整日观察或性能门禁。

D1-P 确认来源：用户回复“D1-P 通过。单字和多字翻页正常。shift+1～5 正常。裸数字键作编码键正常”。只登记明确覆盖的行为；未采集候选正文、编码或截图，不据此宣称未测序号键也通过。

D1-S 确认来源：用户回复“D1-S：5→6，设置重开和 Word 重开后均保留，输入有效”。随后只读模块核对显示 Word 仍是原 PID `7984`，所以“Word 重开”按窗口/文档重入记录而非进程级重启。候选数 6 暂留用于 D2 持久化复核；原值 5 已知且须在 D2 复核后还原。没有读取生产 Rime/PIME 设置或用户正文。

D1-T 证据补全：用户先提供本机观察文件路径；限定字段匹配未能提取位置，未输出自由文本。随后用户直接提供上述数值序列，故将学习提位由待确认改为通过。“遗忘后还原”仅是用户报告的附加观察，未核验遗忘操作类型或作用范围，不据此声称其他学习数据未变，也不把遗忘当作异常恢复或跨重启保留。助手未执行遗忘/清空操作。

## D2 安排与留样（D1 时登记）

以下保留 D1 收尾时的安排和历史观察；2026-09-05 已按用户“开始 D2”启动，当前进度见文末 D2 记录。历史未测状态不追溯改写为通过。

在下一实际工作日继续使用；正常登录/重开宿主后重验焦点、长文本、翻页提交。为判断学习跨 Runtime 重启保留，需要确实发生正常重启并核对新 Runtime/Broker PID/启动时间，不能只关开 Word。无需现在重启；正常重启前自行保存工作。若窗口期间未发生重启，相应结论保持未测，不强行关闭门禁。

固定 D1 模式和输入，先于新的学习比较公开词候选位置。原提位验证样本被用户遗忘后，已要求重新训练公开测试词、记录最终位置并保留，不再执行遗忘。2026-09-04 20:20 +08:00，用户明确回复“D2 学习样本已保留”；按用户确认登记留样完成，不读取本机观察文件，也不推定重新训练后的具体排名。若上下文不一致或未发生 Runtime 重启，保持跨重启保留未判定，不以“输入仍可用”代替学习持久化。比较临时候选数 6，恢复原值 5 并验证。再次采集保护指纹；用户最终明确确认是否可用于本机日常工作。

D1-R 操作前只读进程快照（2026-09-04 20:20 +08:00）：Word PID `7984`，启动于 `16:52:24 +08:00`；Notepad++ PID `35848`，启动于 `17:01:12 +08:00`。用户完成正常退出/重开后再比对新 PID/启动时间及模块，不以窗口重开推定进程已重启。未读取窗口标题或正文。

20:22 补充观察：用户反馈“多次重启都能保持页号和序号”。只读核对当前开机仍为 `12:41:10 +08:00`，Runtime/Broker 仍为 PID `31460`/`31488` 且启动时间未变，Word 仍为 PID `7984`、启动于 `16:52:24 +08:00`，继续加载当前 local.11 x64 DLL。因此保留用户报告，但“重启”的对象、方式或是否指历史操作仍待澄清，不能据此关闭本样本跨 Runtime/电脑重启门禁，也不能补记 D1-R 的取消组合、无候选残留及重开输入检查通过。元数据见 [重启范围核对](testing/l5/2026-09-04-restart-scope-check.json)。

20:31 后续确认：用户明确回复“D1-R 通过，候选数仍为6”；机械核对已出现新的 Word PID `36200`，加载当前 local.11 x64 DLL。因此关闭本日正常宿主重开恢复项，并补充了候选数 6 跨 Word 进程重启保存的证据；20:22 的历史快照仍保留，不追溯改写。Runtime/Broker 未重启，D2 学习/设置跨 Runtime 重启验证仍未完成。收尾证据见 [会话收尾元数据](testing/l5/2026-09-04-session-close.json)及 [保护状态比较](testing/l5/2026-09-04-session-close-comparison.json)。没有新增具体产品回归；未测异常故障恢复、整日实际时长、D2 和最终日常可用确认不补记 PASS。

异常恢复如需主动中断 Broker、备份/恢复或维护操作，另设保存工作后的人工安全窗口；不能在工作期间故障注入，不使用冻结入口，不从 Codex 打包进程执行安装/恢复，不覆盖真实学习数据。原有自动失败写入/延迟锁证据仅作自动覆盖，不替代真实异常发生后的用户结果。

## 隐私与回归记录规则

不读取/保存输入正文、窗口标题、剪贴板、屏幕截图、原始日志、学习库或用户词库；不哈希正文。助手只读取版本、PID、映像路径、当前 Yime 模块、包哈希和保护状态指纹。用户本机观察草稿不进入 Git，也不上传。

只需回报：`D1-F/P/L/S/T/R：通过、异常或未测；应用；大致时间`。出现异常时补充场景 ID、应用/PID、模式、当前 profile 的物理确认、可用合成文本重现的操作步骤、期望/实际及次数。随后关联包哈希和精确加载 DLL；正文不是必要证据。

## D1 日终用户反馈（2026-09-04 20:36 +08:00）

- 用户估计实际累计使用约 2–3 小时，中间有用餐和外出，无法精确计时；不将会话跨度计为使用时间，不宣称连续整日或独占使用。
- 因编码尚不熟悉，持续尝试音元拼音，需快速输入时切换微软拼音；这是用户手动选择当前输入法，不是本轮更改默认输入法。未测量或据此推导产品输入速度。
- 用户报告暂无意料之外的异常。
- 体验观察 `D1-SEG-01`：分词有些不顺。用户认为与词库质量有关；根因未经验证，尚无非敏感固定输入、期望分词/候选、实际行为或旧版本对照，因此暂不认定为已确认回归或已确诊词库问题，也不据此修改词库/分词实现。
- 后续如用户愿意复现，仅使用另行构造的公开合成例句说明期望分词与实际行为，不索取或保存原始工作文本、学习样本或观察文件全文。本轮不额外收集输入正文。
- D1 的本日任务与日终记录完成；D2 留样、候选数 6 保持，正常重启后先复核学习/设置，再恢复候选数 5。未进行的异常故障恢复、D2 和最终日常可用确认继续未关闭，`local_product_ready=false`、`public_release_ready=false`。

已确认新回归：无。已报告但待复现的体验问题：`D1-SEG-01` 分词不顺。待人工核验项如上，不作整个 L5 全部通过声明。

## D2 开始记录（2026-09-05）

用户明确要求“开始 D2”。08:12 +08:00 完成只读启动核对，D2 已开始，但人工场景及本日实际使用尚未确认完成。

- 当前仍为 `0.1.0-local.11`，安装根、manifest 与 D1 一致，74/74 文件匹配；不将本日结果记为旧请求中的 local.9。
- 本次开机为 `2026-09-05 07:34:00 +08:00`，晚于 D1 用户于 `2026-09-04 20:20 +08:00` 确认留样。Runtime PID `8796`、Broker PID `20188` 均于本次开机后 `07:34:34 +08:00` 启动，路径属于当前包，较 D1 的进程启动时间更新。跨真实电脑/Runtime 重启的前提已满足；学习和设置是否保持仍须人工检查，不能从进程元数据推断。
- 12 项生产/历史注册与默认语言设置指纹相对 D1 收尾全部未变。当前 scope ID 的变化来自已批准的平台试验恢复，不是安装身份变化或产品回归。本轮没有执行平台试验、历史冻结载荷、故障注入、安装或恢复操作。
- 08:11 宿主预检未检测到 Word/Notepad++ 进程。须由用户打开专用文档并物理选择当前“音元拼音”，不能把宿主尚未打开或 profile 尚未选择记为回归。后续宿主身份检查只收集进程/模块元数据。
- 证据：[D2 基线](testing/l5/2026-09-05-baseline.json)、[D1→D2 比较](testing/l5/2026-09-05-start-comparison.json)、[宿主预检](testing/l5/2026-09-05-host-precheck.json)。最初沙箱内 CIM 查询拒绝访问；获准的只读查询成功，此工具权限限制不记为产品回归。没有读取私人观察文件、学习库、用户正文、标题或剪贴板，也没有改动产品。
- 此快照只证明当前身份及重启时间，不单独证明登录自启动，`logon_autostart_verified=null`。日终仍需重新核对保护指纹；启动快照不代替整个 D2 窗口证据。

### D2 执行顺序与状态

继续使用 D1 的隔离文档或新建空白 Word 文档；文档隔离不代表学习/设置隔离。先记录当前 T/S 观察，再做新的学习、恢复候选数或其他练习。11:06 用户报告今日为 9、昨日最终值不明；11:32 用户已主动改为 6，实际为 6，但报告设置框总回到 5，当前按文末 `D2-S-UI-01` 处理。此前“保持 9”仅保留为当时安排，不能追溯声称已按 9 完成受控复测。私人留样的具体文本/编码与原始观察文件始终留在用户本机，不收集到日志。

| ID | 本日操作与判定边界 | 当前状态 |
| --- | --- | --- |
| D2-T | 11:47 用户明确当前留样为首页第 6 项 `(1/6)`，补齐此前“位置6”的格式；Word 重开后实际每页 6、位置一致。11:48 Word 仍为 11:34 时的同一 PID/启动时间，故本次按窗口/文档重入保留登记，不推断 Word 进程或 Runtime 已重启。此前各次退出的用户报告保留，不反推全部退出方式。 | Word 窗口/文档重开位置保留 PASS（用户确认，实际每页 6，位置 1/6）；跨 Runtime 保留未判定 |
| D2-S | 用户再次确认今日启动后首次观察单页候选数 9，支持启动后存在非默认候选数的有限结论，不计设置丢失；昨日最终值未知，不能严格匹配最后一次选择。随后用户改为 6、实际 6，报告 Word 退出前后一致，但设置框总回到 5，按 `D2-S-UI-01` 登记 UI 不一致。先保留实际 6/显示 5，不反复应用；退出对象为 Word，UI 回跳时机未补。 | 启动后非默认值 9 有用户观察支持；精确末值持久化未判定；UI 一致性 FAIL；实际 6 的 Word 重开保留为用户报告，还原未完成 |
| D2-F | 用户要求停止 5 次往返，已撤销。12:05 明确补充切应用后原码“变成了普通文本，失去了重新编辑控制线”，补齐原先“大约不能继续”的行为证据。`D2-F-OBS-01` 升级为 `D2-F-01`：未提交编码在失焦后成为不可由原组合续接的普通正文。外部终止源码路径与现象相符；具体现场调用链及首次引入版本未确定，不宣称助手复现。 | FAIL（原码落成普通正文、失去组合编辑控制；用户人工确认）；不再重复测试 |
| D2-L | 使用 D1 的 120 段合成背景，在专用 Word 文档开头、中间、结尾手工编辑，覆盖退格、换行、撤销/重做；正常背景复制不作为输入结果。 | 待执行 |
| D2-P | 单字及多字候选翻页、Shift+1～5 选择与提交、裸数字继续编码。仅记录实际覆盖，不扩记 Shift+6～9。 | 待执行 |
| D2-R | 11:47 用户明确当前重新打开 Word 正常，实际每页 6、留样 `(1/6)`。11:48 核对 Word PID/启动时间未变，按正常窗口/文档重入后的输入恢复通过登记；取消组合及无候选残留专项尚未单独确认，不扩大为全部 R 或故障恢复。因 UI 缺陷暂不恢复 5，不强杀或注入故障。 | 正常 Word 窗口/文档重开输入恢复 PASS（用户确认）；其余专项及异常故障恢复未测 |
| D2-E | 完成本日实际使用后报告粗略累计时长、是否间歇/混用其他输入法及意外异常；重新采集保护指纹。由用户最终明确确认本机是否可日用，不以日期跨度、短测或没有上报异常替代确认。 | 待本日实际使用及收尾 |

D2 启动时已确认新回归：无。`D1-SEG-01` 继续为待非敏感复现的体验观察，未确定根因。异常故障恢复仍未测；D2 启动不关闭 L5/L6，`local_product_ready=false`、`public_release_ready=false`。

### D2 首次人工反馈：最终前值不确定（2026-09-05 11:06 +08:00）

- 用户说明昨天在不同应用间来回测试，已忘记最后退出时的设置值；今天打开时单页候选数为 9，并非其所称默认值 5。用户还观察到昨天一些已调频候选的位置似乎与退出时一致。此处时间是登记时间，不推断实际开应用/观察时间。
- `D2-S-OBS-01`：9 与 D1 20:31 明确报告的 6 不同，但该检查点之后是否再次修改、最终模式/应用、最终退出时数值均未核实。当前只能登记未解释的观察差异，不归因为应用切换、不诊断自动重置/丢失，也不以“不是 5”证明保存成功；设置 UI 的值与候选充足时实际每页数尚需分别确认。D1 当时的 PASS 和原始快照保留。
- `D2-T-OBS-01`：用户的“似乎”属于初步保留观察，不构成精确前后匹配。页大小影响页号/序号，昨日最终页大小未知时不能直接比较；原留样最终名次未补齐，不能从初次且已遗忘样本的 `(1/1)` 代填。真实重启前提已满足，但跨重启学习保留仍未判定，不计 FAIL 或 PASS。
- 此反馈不要求重做整个 D1 或停下全部 D2。需要补齐的是一组已知前后条件的设置/学习持久化证据；不读取私人观察文件、原始输入、词或编码，不据此修改源码或生产 Rime/PIME。

受控复测安排（尚未执行）：

1. 先保持当前候选数 9，不先改回 5 或 6。在 Word 专用文档物理选择当前音元拼音并固定模式，分别核对设置值与候选充足时实际每页数；若两者不一致，先回报应用、模式及两个数值，不继续覆盖现场。
2. 用户在本机记录一个明确已提位的公开测试词及其模式、相同输入/上下文、页大小和页号/序号。若不能确认样本确经学习提位，用另一个公开合成样本记录训练前后位置作为新留样；不要遗忘或清空已有学习。助手只接收数值或“留样记录完成”，不接收词、编码或正文。
3. 用户方便并保存工作后正常重启电脑；助手随后核对较新开机及 Runtime/Broker 时间。重启前记录完成后不再修改该模式设置或训练留样，重启后先按完全相同条件核对设置/实际每页数及留样位置，再继续输入。仅 Word 重开仍不能关闭 Runtime/电脑重启持久化。此安排不代表已执行重启，也不自动调度或中断工作。
4. 复测完成后再通过当前产品 UI 恢复测试前已记录的 5，并核对设置重开及新 Word 输入；如用户明确选择保留 9 等新偏好则记录该决定，不把它伪称为“已恢复 5”。其余 F/L/P/R 和实际使用仍按本日清单继续，只记录实际覆盖。

本次反馈后的结论：新增已确认回归仍为无；D2-S/T 为证据不足待复测，不作整日完成或 L5 放行声明。上午元数据 JSON 保持原样，不追溯改写为新的人工结果。

### D2 设置界面不同步（2026-09-05 11:32 +08:00 登记）

用户补充：“设置改成 6，实际 6，留样位置6，已作记录，退出前后一致”；并明确报告设置候选项数为新值（例如 6）后，设置框总是回到 5，但实际候选框为 6。这里只记录操作/数值，没有采集候选文本或编码。

`D2-S-UI-01` — 候选数设置控件与实际候选数不一致：

- 状态：具体人工验收缺陷，UI 一致性 FAIL（用户报告反复出现，确切次数未提供）；助手未读取界面或执行 UI 重现。首次引入版本及相对旧版是否新增未确定，不把未复现的推测列为其他回归。
- 当前包关联：11:34 只读核对仍为 `0.1.0-local.11`，74/74 清单文件匹配；Word PID `33640` 于 `11:26:39 +08:00` 启动，加载当前包 x64 DLL，SHA-256 `a9f7ceaaa2d7e48ac6779269e94f1a286eecf67d45a0799630f7a5a67eb61900`。这是当时宿主元数据，不替代用户确认触发应用、模式和当前 profile，也不证明曾直接观察设置界面。
- 用户给出的操作路径：打开候选项数设置 → 选择非 5 值（明确例值 6）→ 观察设置控件与实际候选框。
- 期望：设置成功后控件显示已选且实际生效的 6，刷新/重开时也正确回读。
- 实际：实际候选框为 6，设置控件却显示 5。设置框是在选择后立即回跳还是关闭再打开后显示 5，待用户补充。没有证据表明实际每页数也回退为 5、学习丢失、应用退出或生产设置被修改。
- 持久化边界：11:34 的开机时间、Runtime PID `8796` / Broker PID `20188` 及二者启动时间与 08:12 相同，说明本次补留样的跨 Runtime 重启复测尚未得到证据。“退出前后一致”保留为用户报告；退出设置窗口、Word 窗口还是 Word 进程尚未明确。只发现新 Word 进程不能在没有退出前对应快照时冒称严格的前后进程比较。
- 留样只知“位置6”；不推定页号、序号或绝对排名，不把 9 与 6 两种页大小下的位置直接比较。原始 D1 及 D2 上午证据不改写。
- 保护检查：12 项保护注册/默认语言设置指纹较 D2 上午未变。证据见 [设置 UI 反馈时元数据](testing/l5/2026-09-05-settings-ui-observation-metadata.json)；没有读取用户设置文件、学习库、私人笔记、标题、正文或原始日志。
- 本轮只读定位相关实现，不自动修复、构建、安装、修改生产 Rime/PIME 或中断进程；源码分析也不能替代安装版重现及修复后验收。

后续：先保留实际 6 的当前现场，不按错误显示反复改值，也不急于恢复 5。退出对象已于 11:41 补充为 Word（见下方），回跳到 5 的时机仍待补；该 UI 缺陷与设置/学习跨重启保留分开追踪。F/L/P/R 等不依赖设置显示的场景可以继续；D2-S 不关闭，L5 最终日用确认前须明确处理该缺陷。未获修复指示时不改变候选包，避免混用修复前后结果。

#### 同轮只读源码定位（未修复、未执行 UI 测试）

- 高置信度源码原因：`go-backend/cmd/settings-tool/main.go:782` 的 `setComboByText` 在第 786 行向原生 ComboBox 发送 `0x0149`，却把它当作向缓冲区填入选项文字的调用。本机 Windows SDK `10.0.26100.0/um/WinUser.h:11937–11938` 定义 `CB_GETLBTEXT=0x0148`、`CB_GETLBTEXTLEN=0x0149`；长度调用不填文字，故非空数字匹配失败，函数在第 792 行选择索引 0。第 321 行选项次序为 5、6、7、8、9，因此界面落到 5。
- 第 427 行的当前产品 `refreshView` 用此函数显示持久化候选数，第 568 行的应用完成路径再次调用 `refreshView`，与“实际 6、UI 5”相符。源码写入在第 189 行使用请求值，不能因 UI 的 5 就推定写入 6 失败。此为源码静态定位，未对安装二进制进行反汇编或助手实机复现，不把源代码检查写成安装版修复验证。
- 派生风险（尚未人工复现）：`startApply` 重新读取当前控件值，`selectedComboText` 第 801 行使用正确的 `0x0148`；若错误显示 5 后再次点应用，可能真正提交 5。已提示暂不反复点应用，不为验证此风险主动覆盖当前 6。
- 覆盖缺口：`main_test.go:17` 的 `TestExecuteTrialApplyUsesOnlyIsolatedToolbarState` 只验证隔离状态写入/读回 7，未验证原生下拉控件刷新。修复时应补隔离 Windows 控件的 5～9 回读、应用完成刷新、设置重开及不改值再次应用回归；使用测试专属临时状态，不能读写真实用户状态。
- 修复后仍须新候选包身份与安装版人工复验，确保实际值和控件均为非默认值且重开保留；源码或单测通过不能关闭本缺陷。本轮仅更新验收文档和元数据，未修改源文件、构建、安装或执行目标。

### Word 退出范围补充（2026-09-05 11:41 +08:00 登记）

- 用户澄清“退出”指 Word，并表示“应该至少退了两次”。按用户回忆登记至少两次，不将其转成有精确时间/次数的机械轨迹，也不推定每次所有 Word 进程都结束。
- 用户主动提供测试文档路径 `C:\Users\tsaan\OneDrive\文档\首日测试.docx`，仅作人工操作定位。助手未打开、预览、读取、复制、哈希或更改该文件；没有检查 OneDrive 同步状态或据路径推定同步造成问题。该文件不据名称自动认定为已核验的隔离合成夹具。
- 结合上一条反馈，补充为“用户报告 Word 退出/重开后实际候选数 6、所记留样位置一致”。沿用 11:34 当前包/模块快照；本轮没有重新查询进程，也没有逐次退出前后的 PID/启动时间对照。仍不证明 Runtime/电脑重启后的新留样保留。
- `D2-S-UI-01` 继续未关闭；当前只需补充设置控件回到 5 是应用后立即发生还是关闭重开设置后发生，不要求读取文档或重新执行可能覆盖实际值的应用操作。其他未确认的恢复步骤与跨 Runtime 持久化继续待测。

### 启动后非默认值的证据解释（2026-09-05 11:42 +08:00 登记）

- 用户再次指出：今日启动后首次打开为 9，而默认值为 5，因此推断上次设置得到保留。将该判断记录为有实际观察支持的合理推断，不因缺少昨日最终值而抹去其证据价值。
- 可支持的有限结论：已确认今日真实电脑/Runtime 重启；用户报告首次实际单页候选数为 9，说明该次观察不是默认 5，支持非默认设置在启动后仍生效。尚未严格证明这恰好等于昨日最终选择的值，因为最终前值不明，不能追溯补成“昨日最终值为 9”。本条沿用既有机械快照，未重新采集系统状态。
- 设置界面回读错误与实际候选数分开判断：错误显示 5 不否定用户对实际候选数 9 或 6 的观察，也不构成设置未保存的证据。当前具体缺陷仍只有 `D2-S-UI-01`；没有新增“设置重启丢失”缺陷，不将证据缺口记为 FAIL。
- 无需反复追忆昨日最终值或为此重复点应用。精确前后值的持久化结论可在后续安全的受控重启复核中补齐；本日其余人工场景继续，不把这条有限结论扩展为全部设置、学习持久化或 L5 总体验收通过。本轮只改验收日志，未读写用户正文、学习库或产品设置。

### Word 重开正常及留样位置补齐（2026-09-05 11:47 +08:00 登记）

- 用户明确反馈当前重新打开 Word 正常，实际每页仍为 6；留样位置为首页第 6 项，即 `(1/6)`。结合此前退出前后一致的报告，登记为 Word 窗口/文档重开后的实际候选数保留、留样位置保留及正常输入恢复通过（限定范围的用户人工确认）。不保存用户在本条消息中提及的候选正文，仅记录数值。
- 11:48 只读核对：Word 仍为 PID `33640`、启动于 `11:26:39 +08:00`，与 11:34 快照相同；当前加载 local.11 x64 DLL，哈希仍为 `a9f7ceaaa2d7e48ac6779269e94f1a286eecf67d45a0799630f7a5a67eb61900`。本次观察支持窗口/文档重入，不作为新 Word 进程的证据，也不据此否认用户此前其他时点的退出报告。
- 开机时间及 Runtime/Broker PID、映像、启动时间相对 11:34 均未变。证据见 [Word 重开核对](testing/l5/2026-09-05-word-reopen-metadata.json)。没有打开用户文档、读取设置/学习文件、采集标题或正文；本快照未重新采集 12 项保护指纹，不冒称新的全套保护比较。
- `D2-S-UI-01` 仍独立未关闭；正常输入及实际每页 6 不意味着设置控件已回读正确。留样现有明确参考值为实际页大小 6、位置 `(1/6)`，后续跨 Runtime 复核须保持同一模式和上下文且先于新训练。候选正文与编码始终只由用户在本机掌握。
- 当时安排的下一人工步骤为 D2-F 的 5 次往返，未登记执行或通过；用户随后明确不再重复，12:00 已撤销，见下方原码留存观察。不得继续把旧安排呈现为用户待完成的强制次数。

### D2 失焦后原应用保留编码（2026-09-05 12:00 +08:00 登记）

- 用户要求“不再往返 5 次”，并说明早已观察到：在一个应用中输入但未提交时切到另一应用，原应用保留 ASCII 编码，既不清除编码也不转换成汉字。助手停止重复流程，只记录行为类别，不索取或保存真实编码、输入正文、截图或私人文档。
- `D2-F-OBS-01`：失焦后原应用留有编码（用户已观察的行为）。当前具体出发/目标应用、模式及切回后是否可恢复旧候选未明确，不能只因前一步使用 Word 就把所有应用配对的结果都补齐；也不因用户表示早已发现就追溯确定首次版本或覆盖次数。
- 判定关键是旧组合是否仍可恢复：若切回原输入位置时旧候选恢复，选词能替换该段编码，则可属于未提交组合暂停；若编码已成为普通正文且不能再由原组合选词替换，则属于非主动提交的原码遗留问题。尚未获得切回后的这项行为，当前不记已确认回归或 PASS，也不把用户认为有问题的观察忽略掉。
- 助手此前“失焦时正常提交/取消本身不是缺陷”的说明不完整：当前实现还可能保留未提交组合。不能据此推导失焦必定提交首选汉字或必定删除原码；改变这一行为涉及产品策略，本轮未获实施请求，不自行修改。
- 只读源码依据：`YimeTextServiceExperiment/TextService.cpp:371–384` 和 `407–418` 的焦点回调仅更新焦点及候选可见状态，没有在这些回调中提交或删除组合。`tests/TsfCompositionTests.cpp:384–435` 明确检查失焦后组合保留且候选隐藏、切回后候选恢复；这表明可恢复的组合保留是现有测试覆盖的路径，不是本轮助手临时放宽标准。
- 已安装宿主测试的源码还允许宿主终止组合分支（`tests/RegisteredHostTests.cpp:788–823`）；该分支验证重新开始输入，但未在这里严格检查旧原码是否被清除或转换。因此历史 `registered_focus_outcome=host_terminated_cleanly` 标签或已通过的焦点回调测试不能独自排除用户报告的原码遗留。此处仅分析源码，没有运行测试或读取历史运行日志。
- D1-F 的用户 PASS 及原始证据不改写，但不以其覆盖或否定这次补充的具体行为；本日 D2-F 暂不关闭。`D2-S-UI-01` 仍是已确认的设置 UI 缺陷。本轮未改变当前包、生产 Rime/PIME、默认输入法或用户数据，未注入故障。
- 当时仅询问已有观察中切回后能否继续；用户于 12:03 已补充“大约是不能再继续编码”，见下方。原问题不再作为强制重复测试要求；不索取词或编码。

### 失焦后疑似无法续接组合（2026-09-05 12:03 +08:00 登记）

- 用户对切回后能否继续组字补充：“大约是不能再继续编码了”。据此将 `D2-F-OBS-01` 从单纯 ASCII 可见性观察更新为“疑似组合终止后原码遗留、无法续接”。保留“大约”的不确定性，不将其改写成精确次数下稳定复现，也不继续按已确认正常暂停解释。
- 根因线索已作只读交叉核对：`CompositionEditSession.cpp:55–67,108–124` 在组合阶段把 rawInput 写入宿主组合范围；正常提交/取消在 `155–174` 用提交文字/空串替换范围后结束组合。`TextService.cpp:666–675` 的外部 `OnCompositionTerminated` 回调则忽略编辑 cookie，只关闭 UI、释放组合、忘记 context 并在非计划终止时断开 surface，没有在该路径替换旧范围。若宿主终止时保留范围内容，这条路径能够解释原码留在正文且旧组合无法继续；尚未证明用户现场确实经过该路径。
- 现有 registered-host 恢复断言只检查新输入后的末字符，未严格断言旧原码无残留。因此自动测试的历史通过不排除该疑似缺陷，不能凭 `host_terminated_cleanly` 标签替代正文结果验证。没有运行相关测试或读取宿主正文来补证。
- 验收状态：D2-F 不通过放行条件，但仍属待核实问题，不列为已确认回归；当前已确认 UI 缺陷仍为 `D2-S-UI-01`。D1 原始通过报告不改写，也不覆盖这次新补充的行为。完整 L5/最终日用确认仍未关闭。
- 已停止重复往返；不再要求用户重输样本或提供文本。临时使用可在切换应用前先选词上屏，这是避免触发的操作提示，不是修复或通过证明。保持当前实际候选数 6，不在错误显示 5 的设置 UI 反复点应用。
- 若后续明确要求修复，先用隔离合成文档补“宿主外部终止后旧原码处置和返回续接”的回归，再处理产品预期的失焦策略并验证安装版；不能绕过实际 DoEditSession 结果、跨宿主改写或简单强制删除/首选提交。本轮仅文档记录和源码诊断，没有修改实现、设置、生产环境或运行目标。

### D2-F-01 确认：未提交原码落成普通正文（2026-09-05 12:05 +08:00 登记）

- 用户明确补充：“变成了普通文本。失去了重新编辑控制线。”结合未主动提交、跨应用切换后原宿主留有 ASCII、无法继续原组合的前序报告，已能区分正常挂起组合和原码固化。本项由 `D2-F-OBS-01` 升级为已确认人工验收缺陷 `D2-F-01`，D2-F 记 FAIL；保留此前各时点的不确定性记录，不追溯声称当时已确定。
- 可报告的具体失败路径：原应用中组字未提交 → 切到另一应用 → 原应用中的原码变为普通正文，丢失组合编辑控制，不能继续原组合。没有采集实际编码；不推定发生串窗提交、其他正文丢失或所有应用都有相同结果。
- 期望边界：未主动提交的原码不应因切应用而静默固化为失去组合控制的正文；应能恢复原组合，或按明确的产品策略可靠结束。尚未选择自动首选提交、取消或其他失焦策略，不能把这些不同实现当作本轮已授权修复。
- 证据等级：用户对实际现象的明确确认，加上已记录的外部终止路径源码依据；不是助手受控复现，也没有跟踪现场回调，因此具体根因链仍待修复验证。沿用当前 local.11 的已有安装/模块元数据，不声明本条新采集了状态。原 D1-F 的历史 PASS 不删除，但不能覆盖该缺陷或用于当前焦点场景放行。
- 修复验收所需的最小补证：在隔离合成文档中固定原应用/目标应用及模式，覆盖确切的外部终止路径，检查旧原码最终处置与返回后续接，并验证新包的真实宿主行为；这是后续修复验证要求，不是要求用户现在重复 5 次。源码修复、测试返回标签或新输入末字符正常均不足以单独关闭此项。
- 当前未关闭的明确人工验收缺陷为两项：`D2-S-UI-01`（实际每页 6、设置 UI 回到 5）与 `D2-F-01`（失焦后未提交原码成为普通正文）。没有新增“设置重启丢失”结论。两项都未修复，L5 不能记全部通过；助手停止重复操作要求，未改代码、安装包、生产组件、默认输入法或用户数据。

### D2-F 取消策略、源码修复及隔离验证完成（2026-09-05 12:51 +08:00 收尾）

- 用户随后明确选择取消策略：未最终确认的组合在切换应用/输入文档时取消；既不把 ASCII 编码直接落成正文，也不自动选择汉字。只清除原组合的预编辑范围，保留已提交正文。这一新决定取代前述“策略尚未选择”，不追溯改写历史观察。
- 本轮已按该决定修改源码：焦点丢失走与 Esc 相同的空替换编辑语义，不向新焦点应用合成 Esc；宿主外部终止在所给编辑 cookie 下清空原范围，不递归结束组合；延迟取消绑定原组合身份并持有 COM 引用，失效请求不能删除后来新组合或已经确认的提交。文档修改结果仍由实际 `DoEditSession` 完成回报决定，`TF_S_ASYNC` 不当作编辑完成，失败路径关闭候选并断开 surface 供恢复。
- 状态边界：**本次源码修复与隔离验证已完成；当前安装版缺陷尚未关闭。** 没有构建/安装新的可安装候选包，也没有将新 DLL 覆盖进 local.11。`D2-F-01` 为“源码修复通过隔离验证，待新包真实宿主复验”；`D2-S-UI-01` 本轮未修复。D2 整日、L5 最终日用及 L6 不因此通过。

验证结果与可复核证据：

| 范围 | 实际结果 | 不代表的证据 |
| --- | --- | --- |
| 当前身份 x64、x86 WOW64 构建及 Contract | 两架构通过；`YIME_LOCAL_PRODUCT=ON`，使用当前源码的新输出 | 未安装、未运行旧身份载荷，不代表 ARM64 原生验证 |
| 隔离取消编辑单测 | 两架构各 32 项检查、0 失败：同步/延迟取消、重复请求、新组合身份、失败写入与重试、外部终止、确认提交先完成、空范围及拒绝锁 | mock 不能代替真实宿主授锁及回调 |
| 全码/变码/简码 × x64/x86 直接 TSF | 6/6 通过；覆盖失焦取消、已提交前缀保留、新文档新输入、外部终止后无原码残留，以及既有翻页/选择/长组合/标点/中英回归 | 文档焦点回调在直接夹具中显式调用；不是 TSF 自动路由、实际文本锚点或 Word 人工通过 |
| 注册宿主回归 | x64/x86 已编译，包括严格焦点结果、延迟锁与失败写入；原鼠标提交延迟/失败断言保留 | **没有执行**注册宿主测试，没有抢占或关闭用户 Word |

- 最终新输出：`.tmp/yimecore-focus-cancellation/20260905-124645-53fb0adc`。运行中 698 个源文件及文件集合复核未变；10 个测试进程调用均有独立 TEMP/TMP/LOCALAPPDATA 且父进程环境恢复；三个专属内存 Broker 均按精确映像/PID/启动时间清理。汇总、相关源码/产物/日志哈希及夹具元数据见 [源码验证证据](testing/l5/2026-09-05-focus-cancellation-source-validation.json)。不记录用户词、编码、学习库或文档正文。
- 测试辅助安全修补：候选窗口只允许当前测试进程、当前线程拥有的 HWND，并在发消息前复查；隐藏合成探针验证其他线程窗口被排除。直接 TSF 的语言栏注入不启动 Runtime/工具的回调，仍检查设置转换；这不属于自动启动或工具启动验收。临时路径为新 GUID 目录，拒绝复用/重解析点并限制路径长度，避免旧 PID 夹具污染。

失败及隔离限制如实保留，不以最终通过覆盖：

- 首个编译准备因子进程继承了重复的 `Path`/`PATH` 而失败；仅在构建子进程内归一化，没有修改永久 PATH。
- `122512-be854d9e` 在无真实宿主的直接文档焦点检查失败。诊断显示旧组合及候选仍在，直接夹具未送达该回调；补显式回调后严格取消断言通过。真实注册宿主仍要求自动回调，未放宽为“保留或终止都算通过”。这次首个失败停在候选点击和语言栏 OnClick 之前。
- 安全复核发现早期辅助测试存在实际设置读取、全局候选窗查找、可经语言栏发现并启动安装态 Runtime、PID 临时目录复用等隔离缺口；其中后续 scratch 诊断曾走到点击/语言栏段。没有直接证据证明用户文本受影响，但不能据此宣称早期尝试完全隔离。早期 summary 的 `installed_config_read=false` 及笼统无副作用说明不能作为充分保证；原件保留，由本条及源码验证证据补充限定。以上路径已在最终运行前修补，不把最终隔离措施追溯套用到旧运行。
- `123457-a385c978` 在 x86 构建阶段为安全复核主动中断，尚未启动其 Broker/直接测试，无完整 summary；不计产品 FAIL 或完整 PASS。`124219-a915d5f9` 的全码两架构通过，变码在 Broker 启动身份校验处中止并成功清理；最终运行改为重新取得进程元数据，只有映像暂不可用才有界重试，路径/启动时间不匹配仍拒绝执行。

安装与保护收尾：

- [修复前快照](testing/l5/2026-09-05-focus-fix-preflight.json)、[中途快照](testing/l5/2026-09-05-focus-fix-interim.json)及 [12:51 收尾快照](testing/l5/2026-09-05-focus-fix-postcheck.json)确认当前仍为 local.11，74/74 文件匹配同一清单；安装态 Runtime PID 8796、Broker PID 20188 及映像/启动时间未变。12 项生产、旧身份及默认语言/输入保护指纹前后相同。没有读取私人观察文件、Word 文件、候选正文、学习库或 runtime-config。
- 最终运行内独立 StdRegProv 的 19 项保护树汇总前后相同。另行 [注册审计](testing/l5/2026-09-05-focus-fix-registration-audit.json)确认产品 Run 是当前 local.11 Runtime 加 `-no-toolbar`，没有夹具目录或 `-state-root`；双架构 COM、卸载版本及目录正确，19 项树哈希均匹配 9 月 4 日已安装 local.11 的原始证据。早期运行的总哈希与后续总哈希不同，原因未确定，不能单凭总哈希差异归因为修改，也不能宣称所有尝试之间总哈希完全未变。

后续最小验收：先另行安排新包构建及获准安装，再运行新包真实注册宿主回归；用户在合成 Word/Notepad++ 文档中各确认一次未提交切出/切回，报告应用配对、模式及“无原码/无自动汉字、已提交正文保留、新输入正常”即可，不提供正文、编码或截图，不再要求五次往返。安装更新前可在切应用前手动按 Esc 取消；这只是临时避让，不是当前安装版已修复。保持实际候选数 6，不因设置 UI 错显 5 反复应用。最终日用确认仍由用户给出。

### local.12 合并修复与构包完成，原生升级待执行（2026-09-05 13:35 +08:00 证据收尾）

用户询问是否立即构建、重装并重启，随后同意先合并设置 UI 修复及失焦取消修复、验证新包、保存并关闭应用后备份升级，再安排重启。**本次源码修复、隔离回归及 local.12 可安装候选构包已完成；尚未备份真实状态、安装、关闭用户应用或重启。** 已请求用户确认 Word、Notepad++ 及音元设置/工具窗口均已保存关闭，收到确认前不开始安装或重启。本条不把 D2 整日或 L5 总体验收写成完成。

- `D2-S-UI-01` 已补原生隐藏 ComboBox 的先失败、后通过证据。旧实现把读取长度消息当成读取文本，6～9 刷新为 5；隔离状态中的“不改值再次应用”还实际写入 5。该派生问题现在有合成夹具复现，不推定用户真实设置曾经过同样的重复应用路径，也不据此解释昨日最终值 9。
- 修复使用具名 `CB_GETLBTEXTLEN`/`CB_GETLBTEXT` 分别分配及读取完整文本，并检查无选择/无效返回。x64、x86 WOW64 各 16 个顶层测试、10 个子用例通过，覆盖 5～9 回读、真实应用完成消息、重开、不改值再次应用、其他设置保留及长 UTF-16 选项。窗口隐藏且由测试线程拥有，状态在独立临时目录；没有修改 Rime 原生分页、裸数字/Shift 选择约定或真实用户配置。
- `D2-F-01` 的同一取消策略在固定的 local.12 源输入上再次验证：全码/变码/简码 × x64/x86 共 6/6 通过；每架构 32 项取消检查、0 失败，Contract 双架构通过。直接文档焦点回调仍为夹具显式调用；真实注册宿主测试只编译、没有执行，不冒充 Word/Notepad++ 的真实焦点路由通过。
- 两项当前状态均为“源码修复与新包隔离验证通过，待安装及真实宿主复验”，不能关闭已安装 local.11 的缺陷。没有新增确认的用户日用回归。

新包及构建证据：

- 包根：`.tmp/yimecore-local-product/20260905-132513-73c86ac2/package`；版本 `0.1.0-local.12`；清单 SHA-256 `9b3366b350ef2b23fd285fc9d97d876bf1ae6e2fc0f8613c647640f01184502e`。74/74 包文件及完整清单再次核验一致。仍是原生 x64 Runtime/Broker 和当前身份 x64/x86 TSF，不是 ARM64 安装包。
- 700 个源输入与完整集合固定并再次验证；源清单 SHA-256 `2f31f6d278a7fa46e2b7ae2118df88649cefd02ec7a6c3b017d3a5eb099f2c57`。构包保留脏工作树输入快照、差异及源归档，不覆盖或提交用户既有修改，不使用旧安装包作构建输入。
- 正式构包通过 28 项保护注册契约、37 项包内维护契约、5 组无缓存 Go 测试（含设置工具），三模式索引各 1,166,753 项且重复构建逐字节一致，包构建前后及迁移目录独立性检查通过。12 项整句合成回归、三模式合成学习/干净重启/日志恢复、包内合成恢复探针通过；这些都不是用户真实学习库的读写或恢复验收。
- 新包私有 Runtime 的 Broker 故障恢复通过：专属 Runtime PID 2372，Broker 29924 → 30596、重启计数 0 → 1，父进程、精确映像、启动时间和专属管道匹配。已停止本次专属运行进程。正式构包的 7 次及独立焦点矩阵的 10 次测试调用均有私有环境及恢复证据，焦点矩阵的 3 个专属 Broker 均停止。
- 完整索引及 32 个证据文件哈希见 [local.12 构包与交接证据](testing/l5/2026-09-05-local12-build-handoff.json)；新焦点矩阵位于 `.tmp/yimecore-focus-cancellation/20260905-132652-1cef092c`。所有文本用例均是测试自带合成数据，不记录用户候选、编码或正文。

验证限制与保护：

- 设置测试最初遇到缓存权限及夹具线程归属死锁，后者只停止已核准的测试进程；修正夹具后才取得上述旧实现失败和修复后通过证据。它们不是产品日用回归。构建只在子进程内归一化 `Path`/`PATH`，未修改永久环境。
- 构建原始 transcript 有一条新私有 Runtime 尚未生成 `runtime-status.json` 的已捕获读取错误。就绪检查每 100ms 重试、上限 30 秒，随后通过真实 PID/父进程/映像/唯一管道检查；这是初始未就绪探测，不是忽略致命错误或宣称状态文件本身证明运行。
- [13:18 构建前](testing/l5/2026-09-05-local12-prebuild.json)与 [13:32 构建后](testing/l5/2026-09-05-local12-postbuild.json)均为安装态 local.11，74/74 文件匹配；Runtime 8796、Broker 20188、映像、启动及本次开机时间完全相同，12 项保护键指纹一致。构包及新焦点矩阵各自独立 StdRegProv 的 19 项保护汇总前后均为 `e51bcdcd6942271b2abbf030536e0f1e3b6844321a7b47788375a47b0c4a818b`。
- 未改变默认输入法、生产 Rime/PIME、旧身份静态载荷或其注册，未执行冻结历史目标；没有读取用户 Word 文档、私人观察文件、用户学习库或输入原文。测试默认临时位置被替换为隔离目录，不将这类合成恢复证据称为独立用户恢复介质。

本日剩余安排见 [local.12 原生升级与最小复验交接](YIMECORE_LOCAL12_UPGRADE_HANDOFF_2026-09-05.md)：先用户保存关闭，再从 Explorer 启动的同用户普通 Windows PowerShell 5.1 运行旧安装版 Backup，成功后安装新包；使用实际新安装根执行 Verify，不从 Codex 子进程绕过原生上下文保护。实际安装目录可能有冲突后缀，不能把 Plan 的路径当作安装成功证据。当前已知前值仍为实际候选数 6、同一留样 `(1/6)`；真实重启后的精确保持、新包物理焦点行为和最终日用确认尚待用户完成，不再要求五次往返。

### 用户已保存关闭，转入原生备份步骤（2026-09-05 13:42 +08:00）

- 用户明确确认“已保存并关闭”。只读查询未发现 Word、Notepad++、Notepad 或所查询的音元工具进程仍在运行；没有查询窗口标题、正文、剪贴板或命令行，也没有主动终止任何宿主。
- [保存关闭后的升级前快照](testing/l5/2026-09-05-local12-saved-closed-preinstall.json)：当前仍是 local.11，74/74 包文件匹配；Runtime 8796、Broker 20188 及开机/启动时间与 13:32 完全相同，12 项保护指纹无变化。系统原生 Windows PowerShell 路径存在。进程未发现的结论仅限采集时点和所查名称，不宣称所有可能的输入宿主已经全局退出。
- 下一步由用户在同一 Windows 用户的普通原生 PowerShell 中启动**旧安装版** `Maintain-YimeCore-Local.cmd -Action Backup`，成功并核对独立恢复归档后才安装 local.12。助手没有从 Codex 执行维护，也没有重启电脑；当前尚无真实备份成功、安装成功或修复后人工 PASS 证据。
- 13:45 新包只读复核通过：local.12 清单哈希仍为 `9b3366b350ef2b23fd285fc9d97d876bf1ae6e2fc0f8613c647640f01184502e`，74 项内容的长度/哈希与完整文件集合匹配（含清单自身共 75 个文件），无缺失、额外或重复项，当前 CLSID/Profile 及状态命名空间不变。该复核不执行任何新包程序或安装动作。

### 原生备份完成，准备安装 local.12（2026-09-05 13:54 +08:00 核对）

- 用户先报告“找不到文件 &”，经澄清将命令放入 PowerShell 窗口而非运行框；这是启动入口使用问题，没有证据表明输入法、安装包或备份工具发生故障。不将其列为产品回归。
- 用户提供实际 `PS C:\Users\tsaan>` 执行输出：旧安装版维护命令返回 `Quiesced state and package backup complete`，退出码 0。归档为 `C:\Users\tsaan\YimeCore Recovery Archives\local-product-20260905-135008-38219e5a`，须保留作独立恢复介质；不再重复备份或清理归档。
- [原生备份核对证据](testing/l5/2026-09-05-local12-native-backup.json)：只提取清单元数据，schema、备份根、旧安装根、原生上下文、写入者已停止、复制期间稳定及 `passed` 均符合。备份清单 55,144 字节，SHA-256 `317b3bc99d033c5ddf94ea88bda36c1b6922510de5c0a541c3512313ec5ba933`，独立系统文件提供者可见。
- 85 个归档状态文件的完整集合和大小匹配清单，逐文件系统可见性检查通过；没有打开、解析或重新哈希状态内容、私人记录、候选或正文。源/归档逐字节哈希一致的证据来自原生备份流程在停止写入者后执行的检查，不冒称助手做了第二次内容校验。
- 备份会有意重启本产品写入者：当前 Runtime PID `15608`，Broker PID `39576`，分别于 `13:50:19.882679` / `13:50:19.890354 +08:00` 启动且映像仍指向 local.11；74/74 已安装文件匹配，当前运行身份检查通过。较 13:42 的 12 项保护指纹、安装清单和电脑开机时间未变。本次进程变化是正常备份恢复，不是故障或 Windows 重启，不据此补记既有学习留样已保持。
- local.12 新包清单哈希再次与已核准值一致。下一步在用户当前同一个普通原生 PowerShell 窗口运行新包 `Install-YimeCore-Local.cmd /nopause`，需要时允许该安装器的 UAC 提示；安装失败先核对回滚，不强删 DLL 或终止 Explorer。此刻仍未安装 local.12、执行安装后 Verify/注册宿主测试、重启电脑或关闭两项 D2 缺陷。
- 归档中的旧公开安装包另行核验通过：原 package-manifest 哈希与已安装 local.11 精确匹配，74/74 载荷大小及哈希一致；备份清单的 package_files 共 76/76 项（含额外安装元数据）大小及哈希一致。已核验的归档备份脚本只在恢复运行时的真实身份检查和系统可见清单检查通过后输出用户所见成功字符串。本检查未读取或哈希归档 state 内容。

### local.12 已安装；身份/保护核对通过，后续改用不记录文字的复验（2026-09-05 14:08 +08:00）

- 用户提供原生 PowerShell 安装结果：`PASS: YimeCore local product install or upgrade completed`、退出码 0。助手没有代为启动安装、绕过原生守卫或自动重启电脑。
- [14:01 安装元数据](testing/l5/2026-09-05-local12-installed-metadata.json)从 out-of-process StdRegProv 实际解析出安装根 `C:\Program Files\YimeCore Experimental Trial\yimecore-e6c-62af8b507c91-9b3366b3`；版本 local.12，74/74 清单文件完整，清单哈希 `9b3366b350ef2b23fd285fc9d97d876bf1ae6e2fc0f8613c647640f01184502e`。双架构 COM 指向同包 x64/x86 DLL，卸载版本/目录与产品 Run 精确匹配；Run 保留 REG_SZ 类型并带 `-no-toolbar`，不含夹具目录或私有管道。
- 当前 Runtime PID `8844` 于 `13:58:55.8641260 +08:00` 启动；Broker PID `38052` 于 `13:58:56.0112720 +08:00` 启动，父进程为 8844；精确新包映像及当前用户 SID 均匹配。所查 Word/Notepad++/相关工具未在运行。这是安装后启动证据，不是 Windows 重启或 Shell-Core 登录自启动证据，也尚未单独核对 medium-token 运行权限。
- [14:04–14:08 保护证据](testing/l5/2026-09-05-local12-installed-protection.json)：相对备份后的 12 项生产/旧身份/默认输入保护指纹无变化，电脑开机时间未变。当前用户完整产品 TIP 子树（含 Enable 及原始值类型）仍匹配升级前哈希 `96d670fd1e719b772ab3c41b82b13f36616b366822164a4d8752ed53b437a82b`。机器级当前产品两个 TIP 子树哈希变化已逐树解释：每棵树只有一处旧包根变为新包根，即 profile-icon.ico 路径；没有将这种预期升级差异记为回归。
- 新 x64 DLL SHA-256 `a22b5212596b0e6d1bfeada33c5362133b2eecb6a5cbf74eabd650a6e16e6d76`，新 x86 DLL `f5727bfe5c322ef9ceeb1d04c4232b81957d01911e889b770dcbe59c402f7699`。文件确已安装不等于 Word 已加载或物理行为通过；`D2-S-UI-01` / `D2-F-01` 均维持“修复包已安装，待真实宿主复验”。

隐私/测试边界纠正（这些检查尚未执行，不是新增用户日用回归）：

- 暂停并取消原计划的包内 `Maintain ... -Action Verify`。源码会把真实安装态 Broker 响应的首选文字记录为 `modes.first_candidate`；合成请求仍可能得到受用户学习影响的候选，不符合本次不记录文本的约束。因此不先运行再删字段，不访问该报告来补证；也不修改已安装清单内脚本。
- 不原样运行旧 `test-installed-local-host.ps1`：它只隔离 LOCALAPPDATA、未屏蔽继承的工具烟测标志、未强制检查本次新增焦点结果。宿主会请求 `EnableLanguageProfile(TRUE)` 并激活进程 profile，不能把旧 summary 的 `registration_changed_by_runner=false` 泛化为无写入。此处为源码复核，不声称曾在当前安装版发生污染。
- 本轮改为准备独立原生测试入口：先验证当前产品本来已启用，再使用私有 TEMP/TMP/APPDATA/LOCALAPPDATA、专属管道和无持久用户模型的 Broker；保留用户/默认保护前后比较、真实焦点/延迟锁/失败写入结果与仅元数据输出。测试程序会对自有候选窗口模拟鼠标点击，需用户运行期间保持鼠键空闲。该入口在包外，不能当作新安装包内容或实际测试已通过。
- 未读取用户 Word 文档、私人观察文件或用户学习库，未记录候选/编码/正文，未改生产组件或默认输入法，未执行冻结载荷。原生备份、源码隔离通过和当前安装成功分别登记，D2 整日、L5 最终确认及 L6 仍未完成。

#### 独立隐私安全入口准备完成（2026-09-05 14:19 +08:00；实测未执行）

- 已新增包外测试入口 `tools/yimecore/test-local12-installed-privacy-safe.ps1`，哈希 `71bd6440b6d1219995cd299073f61f9e751d58b3ecbb79f35010f0c31655bd95`；没有修改或重新构建已安装 local.12。构包时的 700 项源快照仍绑定原候选，不把包外新入口追溯写入该快照。
- 此入口要求 Explorer 原生 Windows PowerShell 5.1、同 SID 的非管理员 x64 进程和当前已启用的 TIP；原生守卫先于写文件、编译辅助类或启动测试执行。预检只读元数据；完整运行在私有 TEMP 下加载经包清单核验的 token 辅助类，仅读取调用者及当前 Runtime/Broker 的 token，不调用其中的启动器。
- 完整运行隔离 TEMP/TMP/APPDATA/LOCALAPPDATA，清除工具烟测及直接测试环境，固定当前包全部 74 项哈希，使用唯一内存 Broker 管道和合成文档；按 x64/x86 × 三模式要求 17 个布尔标志、正确位数和 `unconfirmed_input_cancelled`。结果只保留白名单，不写任意 stdout/stderr、候选或原始输入。
- 仍如实记录宿主对已启用 profile 的 Enable/进程激活请求，不声称完全无注册写入；总 PASS 必须同时通过当前/生产/旧身份/默认设置及产品 Run/卸载保护的前后比较、日常 Runtime 身份不变、精确自有子进程清理、全部环境变量实际恢复和包复核。异常启动后的进程句柄清理缺口已在交接前修正，独立审查未发现剩余具体阻断。
- [Windows PowerShell 5.1 静态及纯函数证据](testing/l5/2026-09-05-local12-privacy-runner-static.json)：5.1.26100.9168 解析及 8 项合成检查通过，覆盖允许输出、未知文本丢弃、缺失/重复标志、错误/正确位数、超长输出拒绝和摘要稳定。首次证据解析受到子进程 CLIXML 包装影响，改为纯文本输出后完成；不是产品回归，也没有为这些纯函数检查执行维护主入口、原生预检或宿主程序。
- 已交接在用户当前原生窗口启动该入口的命令。运行期间须保持鼠键空闲，因为宿主会在经过所有权检查的自有候选窗执行点击；不再要求用户五次往返。本轮到此只完成入口准备，尚无 local.12 注册宿主、设置 UI 物理、学习留样重启或最终日用 PASS。

### 本次已安装自动复验完成并通过（2026-09-05 14:25 执行，14:29 核对）

- 用户在原生 PowerShell 返回 `Outcome-only evidence: C:\dev\Yime\.tmp\yimecore-experiment\local12-20260905-142517-06e26544`、明确 PASS 和退出码 0。已读取七份白名单元数据文件并独立交叉核对，不重新执行测试，也不读取私有状态或文档。
- 本轮自动复验**已完成并通过**：当前 local.12 的 x64/x86 × 全码/变码/简码共 6/6，通过全部 17 项必需布尔标志、正确位数及 `registered_focus_outcome=unconfirmed_input_cancelled`；各组退出码 0、无超时，六份 outcome.json 与 summary 对应项完全一致。各组的语言栏接受结果也均为 true。
- 本次为真实注册 TSF 宿主回调，不是此前直接夹具显式调用文档焦点：覆盖未确认组合取消、已提交文字保留、切回新输入、延迟取消和失败取消时正文保护；既有按键/翻页/提交、真实候选点击、延迟提交/失败恢复、标点锚点、英文 Shift 和停用后语言栏存活保护均通过。合成宿主的自动鼠标点击不记为用户 Word/Notepad++ 人工操作通过。
- 23 项 StdRegProv 保护指纹前后逐键相同，包括当前/生产/旧身份注册、用户 TIP、默认及排序、产品 Run/卸载信息。测试前当前 profile 已启用；宿主 Enable(TRUE)/进程激活请求仍如实保留，不把指纹未变说成完全没有注册 API 写入。
- 运行器 PID 35592、日常 Runtime 8844 / Broker 38052 均通过同 SID、session 1、integrity 8192、非管理员、非 AppContainer 的主令牌核验；此次补齐了本包运行权限证据。日常 Runtime/Broker 前后精确元数据一致，六组自有子进程清理、全部环境变量恢复与包复核均通过，环境恢复失败列表为空。
- [验收证据与七份文件哈希](testing/l5/2026-09-05-local12-registered-acceptance.json)保留完整白名单结果。summary SHA-256 为 `80d74c77472648ab4135596015b2ca58f81c296451a6c9e627695e071d2f0726`；记录开始时间 14:25:17，summary 文件最后写入 14:25:27。后者只是文件写入元数据，不扩展为长时日用、性能或持续稳定性证明。
- [14:27 实时收尾核对](testing/l5/2026-09-05-local12-registered-postcheck.json)：当前仍为 local.12，74/74 文件匹配，Runtime/Broker PID 和映像正确，未发现残留注册宿主进程；12 项基础保护指纹相对测试收尾仍无变化，电脑尚未重启。没有调用旧 Verify、没有连接日常 Broker 查询候选，也没有记录用户文字。
- 未发现新增确认回归。`D2-F-01` 更新为“已安装修复通过六组真实注册宿主自动复验，待物理应用确认”；`D2-S-UI-01` 仍为“源控件回归通过且修复包已安装，待设置 UI 人工确认”。两项不能仅因本次自动 PASS 就一并关闭。D2 整日、最终 L5 日用确认和 L6 封存继续未完成。

下一人工步骤：允许重新打开 Word 专用测试文档，任务栏物理选择当前音元拼音；暂不改变模式/候选数，查看设置当前显示、设置关闭重开后的显示、实际单页候选数及同条件留样页号/序号。留样只查看，不提交或重新训练；原参考是 6、`(1/6)`，必须报告实际观察而非照抄。若不一致先不覆盖现场。先补齐这一组前值，再安排一次焦点往返和正常重启，不重做五次或整个 D1，也不索取候选正文、编码或截图。

### local.12 设置与未提交切换人工复验已完成（2026-09-05，14:59 元数据核对）

- 用户报告设置显示 7、重开显示 7、实际单页候选数 7，留样为 `(1/6)`；设置候选数一致已测，Word 和 Notepad++ 不提交切换已测，暂未发现异常。本轮这两项人工复验已完成，在用户所报路径范围内通过，不再重复要求五次往返。
- 用户随后补充：候选数核对一致，留样未提交时的位置经换算一致。旧页大小 6 和当前 7 的 `(1/6)` 都是总第 6 位；据此记录用户确认的换算一致性，不再将页大小不同本身当成不可比较或学习丢失。旧数值 6 保留为历史，本次明确重启前基线是 **7 / `(1/6)`**。不推断用户何时/为何改变数值、未重述的模式或昨日最终设置。
- `D2-S-UI-01` 更新为“已安装修复在本次设置/重开/实际均为 7 的用户人工复验中通过”；`D2-F-01` 更新为“已安装修复通过六组注册宿主自动回归，Word/Notepad++ 未提交切换的用户人工复验完成、未见异常”。不把简述扩大为所有模式、所有宿主、具体往返次数或用户未逐项报告的焦点行为布尔值。
- [人工复验与重启前元数据](testing/l5/2026-09-05-local12-manual-pre-reboot.json)：local.12 仍为固定清单，74/74 文件匹配；Word PID 38760 加载当前 x64 DLL，Notepad++ PID 39160 加载当前 x86 DLL，路径及哈希匹配已验证包。只读模块证据不单独证明当下已激活哪个输入 profile，也不是读取或观察用户文档内容。
- Runtime 8844 / Broker 38052 的精确映像与启动时间和 14:27 一致，12 项生产/旧身份/默认输入保护指纹无变化。电脑开机时间仍为 07:34；本轮没有重启电脑，也不能把当前留样一致写成升级后重启保持通过。没有读取私人观察、Word 正文、用户配置、学习库、窗口标题、剪贴板或原始日志，没有运行维护或再次启动宿主测试。
- 未发现新增确认回归。下一步保留当前 7 及同一留样/模式/上下文，不重新训练或遗忘，用户保存工作后手动正常重启；重启后先核对设置/重开/实际均为 7、留样 `(1/6)`，助手再核对较新开机、真实 Runtime/Broker 身份及匹配的登录自启动证据。D2 持续日用、重启保持、最终 L5 用户确认与 L6 封存仍待完成，不因本次人工复验而整体关闭。

### local.12 重启、自启动及候选数保持核对完成（2026-09-05，15:17～15:22 系统核对）

- 用户报告“已重启，数值正常”；候选数按先前明确的 7 记录保持。助手最初把笼统反馈同时理解为留样 `(1/6)` 保持，**该解释被用户随后明确给出的 `(1/7)` 更正，不是最终证据**。
- 用户补充完整操作顺序：留样原为 `(1/6)`；退出前连续提交两次另一个频次靠后的候选，该候选升至 `(1/1)`，留样因此顺延到 `(1/7)`。不记录所选词或编码。此过程改变了 14:59 之后的排序条件，不能再拿较早 `(1/6)` 当作退出前最终值，或把一位移动直接归为重启学习丢失。
- 对退出前是否查看过新位置的追问，用户先给出回忆性限定，随后进一步说明上述顺序。按用户所报操作与当前数值记录为一致，未发现新增确认回归；不补造一份当时已固定的提交后前值，也不冒称本次完成了无新训练的严格留样跨重启对照。当前明确参考为 **页大小 7、留样 `(1/7)`**。不再追问或要求立即再重启；后续自然重启如需补同条件观察，采用实际最后值即可。
- [重启白名单证据](testing/l5/2026-09-05-local12-reboot-outcomes.json)：实际开机 `15:07:41.500 +08:00`，晚于 14:59 重启前记录；Runtime PID 31596 于 `15:08:18.977474` 创建，Broker PID 31672 于 `15:08:19.089404` 创建、父 PID 31596。两者同当前用户 SID、session 1，精确映像属于 local.12；安装文件仍为 74/74 匹配固定清单。
- 独立系统 StdRegProv/HKU 验证产品 Run 完整命令精确指向该安装根的 Runtime，加 `-no-toolbar`，类型仍为 REG_SZ。Shell-Core 9708 的 RecordId `125135`，时间 `15:08:21.4832363`，匹配同 SID、Runtime PID 31596，与真实创建时间相差约 2.506 秒。事件 Command 不包含完整路径，实际已知渲染为 `YimeCoreTrialRuntime.exe" -no-toolbar`；精确匹配此白名单渲染，并以同次开机/PID/SID/时间、当前进程完整映像、独立完整 Run 和包校验建立组合证据。初次拿事件字段直接比较完整路径为 false，属于字段呈现差异，不是产品回归，也未以模糊文件名匹配单独判定通过。
- 12 项生产/旧身份/默认输入保护指纹与重启前相同；当前产品三项 TIP 子树哈希与安装后证据相同，双架构 COM 正常。本次 local.12 电脑重启/登录自启动门槛**已完成并通过**，候选数保持为用户人工确认通过。没有重启/启动/修复产品、修改注册或默认输入法、运行冻结载荷或读取私人文档、用户配置/学习文件。
- 未运行旧 `capture-local-reboot-verification.ps1`，因为它会复制 runtime.log 并保存完整事件内容；未运行合成 E7 回归充当本机证据。只定向读取本次开机、当前 Runtime PID 的 Shell-Core 9708，保存数值、身份和已知命令匹配结果；未保存任意事件 Message/XML，未读取运行日志或 `runtime-status.json`。

本日剩余安排：继续正常日用，不重做已完成的设置/焦点往返或强制再次重启；结束时只需用户报告大致时长、计划外具体异常及是否认可当前候选日用。严格无干预留样比较未成立是证据范围说明，不记为学习故障。D2 最终日用确认、L5 总体确认、L6 封存及其他未执行的原生恢复演练不由本次重启通过自动代替。

### 语流音变后续隔离试验已完成（2026-09-05）

- 按用户“继续推进后续试验，以便重新接入预留语流音变组件”的要求推进，术语统一为“语流音变”。本轮不是重新运行 D1/D2 人工项目，也没有启用已安装产品中的新模块。
- [试验与后续门槛](project/YIMECORE_CONNECTED_SPEECH_RECONNECT_2026-09-05.md)、[仅结果证据](testing/connected-speech/2026-09-05-isolated-reconnect.json)：15 个新索引、三模式共 17,622 条既有模块记录可达；每模式 5 个本调/别名/撤除探针通过；合成 Broker 三模式生命周期合同通过；仓内固定 librime 的 9 个模式子测试通过。本批试验已完成，不等于源码接线、完整进程重启或日用包接入已完成。
- 当前 local.12 74/74 清单、日常 Runtime/Broker 精确身份、12 项生产/旧身份/默认输入注册保护前后不变；固定历史 x86/ARM64 载荷 6/6 静态校验通过，没有执行旧载荷。没有读取用户文本、观察文件、学习库或真实配置，没有安装或重启产品。
- 未发现本轮确认的产品回归。下一批先补规范来源正向派生及三模式严格不增码准入，再做不可变核心/模块代际与 Broker 接线；旧审计的 PASS 不能替代这些门槛。D2/L5 用户最终日用确认及 L6 仍保持待完成。
