# local.12 L5 最小最终确认

日期：2026-09-06；2026-09-08 完成。影响产品：YimeCore；范围仅 MYCOMPUTER 当前安装的 `0.1.0-local.12`。当前状态：用户已完成记事本/Edge 引导日用场景，明确认可本机日用；隐私安全只读收尾快照通过，因此 local.12 的 L5 最终用户确认已完成。L6 不在本次任务范围内，`local_product_ready` 与 `public_release_ready` 仍为 false。

## 用户只需一次回报

正常使用后回复：`使用日期；大致实际使用时长（可估计或未知）；应用名；计划外异常：无/症状类别；是否认可 local.12 可用于本机日常工作：是/否/尚未确定。`

不提供工作正文、输入编码、候选词、截图、观察文件或原始日志。无需重做已完成的设置/焦点测试、留样训练、注册宿主测试或立即重启；不要求连续整日或周末加测。时长只采用用户估计，不从开机时间或会话跨度推算。若反馈有异常，只记录非敏感类别及影响；不把未回复当作无异常。

## 收到正常使用反馈后的一次收尾

助手使用 `tools/yimecore/get-l5-daily-use-baseline.ps1`，固定以下安装身份，默认不枚举宿主：

- 安装根：`C:\Program Files\YimeCore Experimental Trial\yimecore-e6c-62af8b507c91-9b3366b3`。
- Manifest SHA-256：`9b3366b350ef2b23fd285fc9d97d876bf1ae6e2fc0f8613c647640f01184502e`。
- 预期版本：`0.1.0-local.12`；预期清单：74 文件。

只采集时间、机器/版本、安装路径、清单及包文件哈希、Runtime/Broker PID/映像/创建时间、开机时间、当前 x64 COM 映像和 12 项保护注册指纹。进程查询从提供程序端限定字段，不读取 CommandLine。注册通过独立 `StdRegProv`，错误时不回退进程注册视图。不调用维护/Verify、UI、截图、事件或日志收集入口，不访问用户设置/学习/状态文件，不启动、停止或重启产品，不修改默认输入法或任何注册。

与 [local.12 重启证据](testing/l5/2026-09-05-local12-reboot-outcomes.json) 的 `current_installed` 比较精确版本、manifest、文件数、完整性、当前运行身份、COM 和全部 12 项指纹。仅保存白名单 JSON 及比较结果。清单不匹配、保护变化或元数据查询失败应如实登记并保留未关闭状态，不自动修复/重装。自然产生的新 PID/开机时间单独记录，不因变化直接判产品回归；本次快照不生成新的登录自启动通过结论。

快照只证明采集时的状态及与历史端点的比较，不证明期间每一刻、用户实际输入的内容、活动 profile、应用覆盖或输入性能。无须为得到宿主模块证据要求用户重开应用；若以后为具体问题核对模块，另按明确应用/PID 限定。

## 精确结论边界

将用户回报与机械核对分栏追加到 [L5 日志](YIMECORE_L5_DAILY_USE_TEST_LOG.md)，保留日期、实际时长/未知、应用、异常与是否认可日用。只有收到明确认可且收尾身份/保护检查通过，才登记本机 local.12 的最终日用确认；请求“完成验收”本身不是使用结果。

- D1 的 local.11 约 2–3 小时间歇使用及定向结果仍是历史证据，不改写为 local.12 或两个完整工作日。
- local.12 已有六组 x64/x86 注册宿主、所报 Word/Notepad++ 失焦与候选数 UI 修复复验、2026-09-05 重启/登录自启动和候选数 7 保留结果沿用原证据，不声称本次重跑。
- 不把一般日用认可扩记为 D2-L/P 专项、Shift+6～9、全模式、异常故障恢复、性能测量或新的原生恢复演练通过。
- 留样 `(1/7)` 与用户所述退出前调频过程一致；无干预严格跨重启对照仍未建立。D1-SEG-01 分词体验待复现，不擅自归因、关闭或修改词库。
- 最终认可可作为 L6 的一项输入；未执行项目保留未测/限定范围。L6、`local_product_ready`、`public_release_ready` 均不因本次确认改为完成/true。
- 本次不涵盖未安装 local.13、Rime/PIME 维护、双产品维护矩阵、ARM64 或其他机器的日用验收。

## 2026-09-08 完成记录

- 用户按引导在新的空白记事本中完成普通组合、候选提交、短文本/标点输入，并在记事本与 Edge 间做一次焦点往返；随后报告应用为记事本、Edge，计划外异常为“无”，明确认可 local.12 用于本机日常工作。用户把时长写成 `约X分钟（从测试开始一直到现在）`，因此只记录实际区间描述，具体分钟数保持未知，不据对话或进程时间推算。
- [最终日用报告](testing/l5/2026-09-08-local12-final-daily-use.json)将用户回报与机械核对分栏保存；没有索取或记录正文、输入编码、候选文字、截图、观察文件或原始日志。助手先前的浏览器自动化键路径只产生原始合成数字，未建立 TSF/profile 激活，明确不计入人工日用通过依据。
- [使用后只读快照](testing/l5/2026-09-08-local12-final-post-use.json)仍为 `0.1.0-local.12`、固定安装根及固定 manifest，74/74 文件完整；Runtime/Broker 映像和启动身份正确，x64 COM 指向当前包，12 项保护注册指纹与 2026-09-05 重启证据精确一致。使用前后进程元数据及保护指纹也完全一致。
- 本机 local.12 的 `D2_daily_use_final_confirmed` 与 `L5_final_user_confirmation` 现为 true，未发现新增确认回归。该结论不补造其他应用、全模式、Shift+6～9、性能、异常故障恢复或新原生恢复演练通过；L6 封存和两项 ready 标志保持未完成/false。
