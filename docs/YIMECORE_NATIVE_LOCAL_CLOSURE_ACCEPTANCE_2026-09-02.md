# 本机原生数据安全与安装回退闭环通过

2026-09-02 22:21:43（北京时间），当前 Windows 用户在 Explorer 启动的独立 Windows PowerShell 5.1 中完成原生备份、实际恢复及真实失败升级回退。随后只读复核 13 项全部通过，关闭此前因打包应用隔离视图而待补的本机恢复/回退证据缺口。

范围仅 MYCOMPUTER 原生 x64、当前 YimeCore 并列试验产品。ARM64、x86、老旧 x64、其他实体机与硬件模拟仍冻结，未宣称通过；没有更改生产 Rime/PIME 或用户默认输入法。

## 已验证结果

- **原生停写备份**：实际恢复前、失败升级前各保存一份完整状态和旧安装包，均在 AppData 外。只读复核 302 个归档文件的系统可见性、长度和 SHA-256 全部一致，归档中的完成摘要与运行证据同哈希。
- **实际恢复**：6 个模型/词库/设置文件真实恢复到原生用户目录，恢复后哈希与备份一致；恢复前 6 份原件另行保留并再次核验。备份后新增或修改数据的保护仍启用，不强制覆盖更新的学习数据。两份日志恢复 generation=485/335、学习记录=165/97，截尾字节及恢复相关失败计数为 0。
- **真实失败升级回退**：使用故障专用包进入实际安装事务，触发 `trial runtime did not become ready within 15 seconds`，安装器退出码为 1，旧包自动恢复。该退出码是预期故障注入；不是本轮验收失败。回退摘要 10 项检查全部通过。
- **系统注册保护**：原生前后完整 COM/TIP、用户 TIP、Run、卸载信息、语言配置及默认输入法一致；原始注册值类型一致。独立 `StdRegProv` 当前再读与原生结束快照一致。默认仍为微软拼音，生产 Rime/PIME 注册保留。
- **包和运行身份**：恢复到 `yimecore-e6c-45b389e530c0-8d48953a`，manifest SHA-256 为 `8d48953ac0b5017b725272ee6300d0b988e99a0d25b9e035216f6c90b774fb64`，62 个安装载荷重新核验通过。原生结束证据确认 runtime PID 13132、Broker PID 3536，路径、SID、父子关系、启动时间及三模式查询通过。当前非提权观察可确认同一 PID、父进程和毫秒级创建时间；无法直接读取提权进程的映像路径，不把权限限制当成服务故障，也不以旧状态文件替代原生证据。
- **冻结架构**：故障演练只调用 x64 注册工具。冻结 x86/ARM64 包文件从已核验基包原样继承并记录来源，未构建或执行对应工具；注册的静态保留比较继续执行。

## 证据与恢复介质

运行证据根：`C:\dev\Yime\.tmp\yimecore-experiment\native-closure-20260902-222101-add18338`。

- `summary.json`：原生完整执行摘要；SHA-256 `5e73ff2377c66f1cd0e200362ab6fc46a92a40d88902b97db06876903fb782b0`。
- `reviewed-summary.json`：后续只读 13 项复核；未执行新的维护操作。
- `rollback/summary.json`、`rollback/before.json`、`rollback/after.json`：预期故障、退出码和 10 项回退检查。
- `before.json`、`after.json`、`review-current-state.json`：原生前后及当前独立系统注册观察。
- `autostart-after.json`、`uninstall-after.json`：系统视角只读验证。

独立恢复归档：`C:\Users\tsaan\YimeCore Recovery Archives\native-closure-20260902-222101-add18338`，含 `restore-backup`、`rollback-backup`、恢复前原件及 `closure-summary.json`。此前失败运行的归档和记录保留，未覆盖或删除。

## 结论边界

结合既有真实 Word、记事本及生产输入法回退输入记录，本机并列试验版的本轮功能/宿主、数据安全、失败升级回退补验已闭环。之前 21:29 重启后的登录自启动验收仍单独保留；本轮没有再次重启，维护后的手动启动不冒充新的登录自启动证据。

不必再次运行本轮补验或升级脚本。该结论不批准首次独立发布、不改变默认输入法、不卸载生产版，也不代表未来不同独立产品身份的迁移/退役回退已经验收。

签名证书正在申请，等候审批，暂缓相关事项。
