# 维护收尾进度：重启后两套卸载成功

包：maintenance-5c9a5d77；日期：2026-09-14。

用户反馈：“重启后卸载两套成功。”

- Rime/PIME：859494cf405b471b98f5378e4a91c8f9，18:00:13 完成卸载，父进程收到退出码 0。
- YimeCore：1955baca75ea4ba6970df1b2bcac7636，18:00:22 完成卸载，父进程收到退出码 0。
- 重启前 17:54 的 be50b0474d9243b4a0b386e13c0a5798：Rime DLL、字体无法独占读写，在等待释放阶段取消，退出 2。该受阻尝试保留，不由后来成功覆盖。
- 本次额外只读检查各自安装目录，结果见 directory-observation.json；此为当前工具文件系统视图。安装条目 UI 未另行核验，不将卸载日志等同于独立 UI 核验。

## 接续 NEXT-MAINTENANCE 的状态

1. Core 卸载后 Rime 可输入、Core 装回正常：已有用户确认和日志。
2. Rime 重启后卸载、留下 Core 可输入、Rime 装回正常：已有用户确认和日志；此前受阻记录保留。
3. 重启后两套卸载操作：通过（用户反馈和父子日志）；目录检查见附表，安装条目移除未单独确认。
4. 在另一套未安装时，仅装 Core、输入后卸载：未测。
5. 在另一套未安装时，仅装 Rime、输入后卸载：未测。

当前两套已卸载（据日志及用户反馈）。尚不能记作完整维护矩阵完成；单独安装验收仍待进行。默认保留用户数据；本轮代理只读取日志/目录并归档，没有执行安装、卸载或修改包。历史报告保留。
