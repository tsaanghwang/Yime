# 第二轮清理与分支交接

影响产品：YimeCore、Rime/PIME 的旧维护辅助接口，以及开发交接文档。当前简版安装、文件占用提示和产品归属机制保持。

本轮移除旧安装维护使用的 Runtime/Broker 独立健康管道、PIMELauncher 定向停机协议与专用后端退出分支、Rime readiness 请求，以及相应孤立测试。正常输入管道的用户身份认证、连接配额、会话回收、普通进程重启和引擎日志重放仍保留。

语流产品测试不再要求已删除的包内恢复程序，改用正常用户模型实现读取自有合成状态副本；回归确认日志中的学习代数可重放，原始夹具文件不变。这是测试进程内检查，不是安装包恢复验收。

清理 AGENTS、测试指南、发布指南、旧网页入口和 Git 属性中的过期事务要求、失效命令与旧 CI 矩阵。设置工具仍调用的轻量手动备份、引擎持久化日志、Rime 字典部署和离线规范数据恢复属于实际功能，保留；它们不成为安装前置条件。原始历史证据不改写，退役代码可从 Git 历史查询。

开发说明与测试数据恢复通过 `codex/yimecore-replacement-experiment` / `perf/i7-7820x-local` 交接。四组有效维护报告及日志迁入 `raw/`，原有 67 项 SHA-256 核对通过。旧共享目录保留归档，入口标记停用。当前无新二进制交付，不要求测试端重复上一轮安装。

本地验证：Go stable 全量 test/vet 与关键守卫通过；固定 i686 host Rust 15 项单元测试和 2 项集成测试通过（覆盖断连、崩溃重启、阻塞后端、分片消息）；构建契约与 CI 调度检查通过。新增的副本重放和 Runtime 管道名校验回归通过；GOARCH=386 的 Broker、Runtime 与命名管道整包测试通过。用 Go overlay 排除预先存在的未提交改动后，语流测试工具和 Runtime 的暂存版本测试通过；Git 暂存中的 67 项归档哈希与原始值一致，diff 检查通过。

本次没有改动开发机或测试机已安装状态，没有新包原生安装/输入验收。`5c9a5d77` 实机记录只适用于原包，不能移用于本次运行程序改动。新包仍须在开发端完成相应验证并通过对应 CI 后，再交测试端执行。
