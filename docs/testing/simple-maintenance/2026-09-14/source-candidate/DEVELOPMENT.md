# 清理后源码候选包：开发端验证

状态：2026-09-14 已完成源码构建、完整制包、隔离维护检查和开发机两套产品替换安装。安装文件哈希、双架构 COM 注册、运行进程完整路径检查通过。用户随后确认：“重启前后都能正常输入!”。两套新源码运行程序的开发机输入验收通过；首次注册冲突作为已知维护注意事项保留，尚未交付测试端。

本机首次执行：Rime/PIME 成功；YimeCore 在旧副本卸载成功后，重新注册返回 `0x800700B7`，安装退出 1。之后两种架构只读查询均显示 COM、Profile 和类别注册已不存在，单独重试 YimeCore 安装成功。用户提出卸载后可能需要重启；这符合注册状态延迟释放的可能性，但该次成功重试之前未重启，尚不能断定具体原因或必需重启。遇到同类“已存在”冲突时，保存工作并正常重启，再运行同一完整包；不清理无关注册，不接续历史恢复链。该首次失败仍保留，不计为一次安装成功。

原始安装日志标识：Rime/PIME `ec0a768f8e8d44529ea60d8f1045ab65`；YimeCore 首次 `c8638b77e7704e80aa30aac6113531f1`；YimeCore 重试 `c03f030672724eea94f2c85e9b83b47b`。三份管理员日志、安装后只读核查 JSON/日志、源码构建汇总已按原始字节归档到本目录 `raw/`，大小及 SHA-256 见 [证据索引](evidence-index.json)。普通权限不可见的进程路径已经提权核实。原始 JSON 写于用户实测之前，其中输入和重启的 `pending` 保留原样，后续用户确认见本文状态。

## 构建与修正

基础提交为 `58e360a52e83549a124ede2d5ab288281843b03e`，候选包包含本轮工作区修正，不应把基础提交的 CI 成功当成本候选包通过 CI。

- Rime/PIME：通过 `Build.ps1` 构建 x64/x86 PIME、固定 i686 Rust 工具链的 Launcher、Go 后端及工具，包含固定的 Rime 依赖。
- YimeCore：通过源码构建入口生成 x64 Runtime/Broker、x64/x86 TSF、三模式词库索引及原有 Stage5C 语流资源。三模式索引各构建两次，字节一致；两种架构契约与焦点取消检查、Go 核心测试、独立性审计通过。
- 修正语流准入入口残留的旧安装基线依赖：源码构建使用只读系统注册检查，不再要求旧安装副本；存在的历史文件仍只读核对。
- 补齐导出器对 `build-system-observation.ps1` 的源码清单，并增加准入入口与导出器清单一致性回归。语流规则和词库内容没有修改。

两套包均通过文件完整性检查。隔离维护检查覆盖私有字体占用、另一产品与无关目录保护、损坏副本重装、两套副本卸载和中断复制清理；这些结果不代表真实宿主输入已验收。

## 本地候选包

以下路径仅是开发机本地构建输出，不是共享目录交接指令。

- ZIP：`.tmp/fresh-products-20260914/Yime-Source-Candidate-20260914.zip`
- 大小：256617810 字节。
- SHA-256：`8474875273b42404e8d1e6a29206216a329aca44e6b01499a0e749170ae3021d`。
- 解压入口：`Install-Uninstall.cmd`，支持单选或两套产品安装/卸载。
- 包内 `BUILD-PROVENANCE.json` 记录两套产品清单哈希和 YimeCore 源码快照哈希。
- 本地构建证据：`.tmp/yimecore-local-product/fresh-20260914-r2/summary.json`、`transcript.txt`；语流准入目录 `speech-admission-20260914-220443-6c76453a22f847398f49c69a2bd1cce0`。

首个 YimeCore 构建因两端源码清单不同步被拒绝，其输出保留但未制包、未安装。重新构建的 `r2` 已通过。

## 后续顺序

1. 开发机保存文档、退出占用应用后，使用完整候选包替换安装两套产品。
2. 核对安装文件哈希、双架构注册与运行进程，再确认真实输入和重启后输入。（已完成。）
3. 提交已验证的构建修正和验收记录，等待对应提交 CI。CI 成功不代表首次注册冲突已解决。
4. 保留当前已正常工作的开发机安装，不为单次冲突反复卸载。测试端在 CI 成功并收到完整包后执行本轮源码新版的安装、输入与重启确认；同类失败按上述维护注意事项处理并如实记录。
5. 交付时发布完整 ZIP 的 GitHub artifact/Release 下载地址与 SHA-256 到 `installer/simple/HANDOFF.md`，测试端再下载执行指定验证，通过测试分支返回报告。

本轮不要求测试端重新执行历史恢复矩阵。生产数据迁移、备份与恢复仍未纳入本轮。
