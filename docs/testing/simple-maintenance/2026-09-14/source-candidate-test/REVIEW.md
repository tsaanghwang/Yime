# 开发端复核：本轮源码候选包验收完成

收取测试分支最新两次提交 `85aec9c4`、`7ab9566f`，以及它们引用但此前尚未归档的收包记录 `50f70a9c`。仅合入这三次报告提交，未引入测试端其他源码。

复核结果：

- 包大小 256617810 字节、SHA-256 `8474875273b42404e8d1e6a29206216a329aca44e6b01499a0e749170ae3021d`，与发布的已验收 ZIP 一致。
- 两份证据索引共 10 个原始文件，逐个复核字节数和 SHA-256 全部匹配。
- Rime/PIME 与 YimeCore 管理员日志均记录安装完成，父进程均收到退出码 0。YimeCore 两项注册均返回 `hresult=0x0`，本轮日志未出现开发机首次注册的 `0x800700B7`。
- 用户确认两套输入法在 Codex 与 Notepad（记事本）中，重启前后均可正常输入。真实输入及重启结论来自用户实机确认；不把脚本检查当作输入验证。
- Word 未报告结果，不记为通过；没有推定记事本进程架构或补做额外宿主测试的要求。

结论：本轮从清理后源码构建的双产品候选包，在开发机与“计算机”测试端的已报告范围内验收完成。测试端保留两套安装，转入正常使用，不重复历史维护矩阵或最终卸载。开发机首次注册冲突继续保留为维护注意事项，测试机成功不能证明其原因已经确定或普遍消失。

原始 `TEST-RESULT.md` 的 pending 是接收时状态，保留不改；后续结果以 `INPUT-REBOOT-RESULT.md` 和 `APPLICATION-CONFIRMATION.md` 为准。
