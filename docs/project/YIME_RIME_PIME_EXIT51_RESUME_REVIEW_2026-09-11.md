# 单次 Resume 失败：开发端复核与补证交接

> 已收到 `5ef571a61` 补证。本页收集任务已完成，下一步执行[只读绑定诊断](YIME_RIME_PIME_EXIT51_BINDING_DIAGNOSTIC_2026-09-11.md)，不要重复收集或恢复。

影响产品：Rime/PIME 候选恢复；YimeCore 为保护对象。

## 复核结论

已接收测试分支提交 86f4e96b50aad4119fd4c63724727f9dd85f6668，核对失败报告和 evidence-index.json。唯一一次 Resume 返回 51；191 项载荷前后仍在且身份、大小、哈希匹配；安装和移除 terminal 均缺失。回滚仍未闭合，原单次恢复方案已执行且失败，不再作为可执行指令。

索引中前后 payload、registry、default、process、decisions 记录的摘要分别一致，新授权 peer 比较 unchanged=true。这支持所报告观察时点的保护状态，不代表安装或恢复验收通过，也不能证明过程中没有瞬时操作。开发端只复核了已提交报告和索引，未读取测试机 Git 外原始文件，不能把索引复核写成原件哈希复验。

## 诊断缺口

退出码 51 是通用错误，当前不能判定控制器或提升 worker 的具体失败行。新源码诊断未进入固定旧包，CI 成功也不会为旧包补回异常。

源码 Read-MaintenancePreparedPlan 除 journal 决策关联外，还检查完整计划结构与字段类型、新授权的七个绑定字段、journal 路径、install/state 目录身份及恢复 EXE 身份。此前报告未逐项给出这些检查的证据；它们是下一步排查范围，不是已经确认的根因。尤其不能直接对原 journal 调用该函数当作只读探针：Open-CandidateJournal 会以 ReadWrite 打开 transaction.lock 并 Flush。

## 给测试端 AI 的下一步：只交回现有证据

本轮不重新运行 Install、Remove、Resume 或生成新授权，不替换原包模块，不删除载荷或补写终态。保留原包、票据、journal 和新旧授权。

1. 从已经保存的 console.txt 提取完整错误段及其前后阶段信息，脱敏后提交到本报告相邻目录；保留原文件与哈希。若确实只有外层 51，明确记录，不为获取更多输出重跑维护。
2. 提交本次本机辅助脚本的脱敏副本及其 SHA-256，包括前置数组修正与 37 项接受、36 项拒绝的验证方法。移除授权、SID、MachineGuid、token 和个人绝对路径，用占位符说明替换位置；保留真实 PowerShell 表达式与调用顺序。只读采集和执行 Resume 的入口须区分标识，副本仅供源码审阅，不能作为执行入口。
3. 从已有解析结果或已归档副本汇总计划元数据：顶层字段名及 PS5 解析后的类型，files/generated_files 的实际数组类型及数量，default_input 三个字段的类型（不发布值），product_version，以及目录身份和恢复 EXE 身份此前是否确实核验。未核验就写未核验，不自行编写会打开原事务锁的探针。不要提交完整 prepared、票据、授权或用户数据。

原始资料继续留在仓库外。脱敏文本与结果提交到 perf/i7-7820x-local；开发端据此审阅旧包解析/入口差异，再设计不运行维护分支的诊断桥接，或绑定原事务的专项恢复。当前文档不授权新的恢复操作。

## CI 与测试安排

以上是已有文件的读取、脱敏和源码提交，可以立即进行，无需等待本次文档提交的 CI。安装与恢复继续暂停；任何未来恢复工具需先完成针对性验证和对应 CI，再单独交付具体执行步骤。不要将历史恢复方案中的命令再执行一次。
