# SR4-B2：默认关闭的正常语流候选包与私有进程验证

完成时间：2026-09-05 22:24（MYCOMPUTER）。**本轮构包与隔离流程测试已完成并通过**，承接 [SR4-B1](YIMECORE_SPEECH_SR4B_SOURCE_2026-09-05.md) 和[双独立产品计划](YIME_DUAL_PRODUCT_DEVELOPMENT_PLAN_2026-09-05.md)。影响面仅为 YimeCore 构包、审计、测试夹具及新增语流开关的维护数据枚举；规范词典、共同规则和 Rime/PIME 产物未改。

后续状态：23:02 的 [`SR4-B3`](YIMECORE_SPEECH_SR4B3_MAINTENANCE_2026-09-05.md) 修复了本记录已明确保留的 `learning.json` 维护缺口并重构同版本候选；随后 [`SR4-B4`](YIMECORE_SPEECH_SR4B4_SOURCE_SYNC_2026-09-05.md) 又把 DP1 共享源码变更纳入当前来源闭包。下列 B2 包继续作为当时的历史证据，已被 B4 的 package ID／manifest 取代，未安装、不得当作当前安装交接包。

新候选为 **local.13，默认关闭语流音变，未安装**。日用仍是 local.12。本轮不是完整 SR4、安装／维护事务、注册宿主、Windows 重启或 L5 最终确认。完整计数、精确路径、57 个证据文件的大小及 SHA-256 见[结构化结果](../testing/connected-speech/2026-09-05-sr4b-product-package.json)。

## 实现与来源

- build-only 导出器只生成 11 个新增文件：9 个准入收据／记录／索引逐字节复制，2 个产品 envelope 新建。包外映射保存来源与目标摘要。全部 56 项正向来源文件、576 项当前源码／明确输入、13 项准入产物、工具哈希和准入七阶段均复核。
- 正常三模式核心索引仍各自从规范词典独立构建两次；两次字节相同，且分别等于准入核心。索引与布局不匹配会拒绝导出，不把旧索引当作当前构建。
- 可选 `speech` 描述严格默认关闭。独立包审计同时校验外层清单、PE 架构与依赖、固定 11 文件、原始来源链、记录／布局和三模式注释绑定。没有能力声明的旧 local.12 合同保留；未声明、未知或损坏语流文件不能静默进入包。
- 包内 `build/build-inputs.json` 只保存准入、源码、包外导出映射、capability 和 product 的固定摘要；不引入仓外运行路径。Python、准入／七阶段工具、试验状态、私有缓存和来源文件不进入安装载荷。
- `speech.json` 补入实际维护数据枚举，新增／改变／删除均能拒绝陈旧恢复证据，实际 restore 数据循环的目标映射在夹具中验证。真实备份／恢复没有执行。

准入根：`C:\dev\Yime\.tmp\yimecore-experiment\speech-admission-20260905-221859-ca9bf2f13d96471a92299db7cb8536d6`。准入 summary SHA-256：`736f7f2723162c2397c2c6200f38f5d0209e2ea07d33c15e43ab441c3c1dcad0`；576 项源码清单 SHA-256：`28c3f08dc1cb6341e794a3e257dc45c67a6c3624385138f8c36b0ed7146700dc`。

候选根：`C:\dev\Yime\.tmp\yimecore-local-product\speech-20260905-222050-7c3a4241\package`。产品 ID：`yimecore-local-0.1.0-local.13-50d4d4692f27`。外层 manifest SHA-256：`0023a719b147754ba50a55cab50ac9c139ba5bbc81bb6b10a757018104f33681`，载荷共 **85 文件**，包含新增 11 文件。正常构包另外固定 760 项来源及源快照；源码修补后的构建均使用新根，没有改写或复用上一候选的验收结论。

## 验证结果

| 层级 | 实际结果 | 不代表什么 |
| --- | --- | --- |
| 新鲜准入 | 497 个 Go 命名节点通过、0 FAIL、3 SKIP；Python 正向源合同、三模式准入及私有 stdio Broker 流程通过 | 不是正常安装版或宿主 |
| B2 定向 Go | 6 包共 477 个命名节点通过、0 FAIL、4 SKIP | 与准入、builder 测试有重叠，不相加制造总数 |
| PowerShell 合同 | PS5／PS7 各 20 项构包预检＋6 项语流维护数据合同通过 | 不是实际安装或恢复事务 |
| 正常构包 | x64／x86 当前身份原生构建，焦点取消各 32 检查，5 个 Go 包、28 项注册保留合同、37 项包内维护合同通过 | 原生注册工具只构建，不执行注册 |
| 私有直接 TSF | x64／x86 组合、取消、分页／选择、长会话等通过；12 组句例、三模式学习与克隆恢复通过，监督器只重启自己的故障子进程 | 未调用安装的 TIP、Word 或 Notepad++ |
| 仓外正常进程 | 下表七阶段全部通过；原包及私有有效副本事后完整性保持 | 不是 Windows 重启、已安装维护或人工日用 |

仓外根：`C:\Users\tsaan\YimeCore Isolated Fixtures\SR4B2\speech-product-test-20260905-2220-a2`。六次正常 Runtime/Broker 启动各自核对 PID、父 PID、映像、创建时刻、SID 和管道服务端 PID。所有阶段使用明确的私有路径和唯一具名管道，不连接日用管道。正常程序从包中启动，测试工具留在包外。

| 阶段 | 结果 | 学习代数／关键证据 |
| --- | --- | --- |
| 默认关闭 | 通过 | 0；三模式各审定记录的规范路线有效，无静态语流来源 |
| 显式开启并选择 | 通过 | 6；每模式两次明确选择 |
| 正常进程重启 | 通过 | 6；只读取原开关，不重写设置 |
| 关闭 | 通过 | 6；静态模块撤除，已有明确选词的学习保留 |
| 再开启 | 通过 | 6；三模式恢复，规范拼音注释保持 |
| 损坏能力副本 | 通过 | 精确摘要错误、退出码 1、无超时／取消、原状态所有文件摘要不变 |
| 有效包恢复启动 | 通过 | 6；只读取原设置，三模式及学习恢复 |

每个正常阶段核对 72 项规范路线与 72 项别名／关闭来源；学习代数由正常进程停止后新建的克隆调用包内 RecoveryProbe 验证，原模型摘要前后不变。Runtime 的正常 stop 会终止其 Broker，**不把它称作 Broker 优雅检查点关闭**。坏资源阶段表中的代数不另行修复／探测，仅证明原状态未变。

## 本轮具体修正和未通过记录

1. 新增语流设置未进入原维护数据枚举。隔离红灯 6 项中 5 项失败；补入 `speech.json` 后 PS5／PS7 各 6 项通过。本次仅补新增文件的枚举／新鲜度和数据复制映射，不把它扩大为整个维护事务已通过。
2. 首轮正常候选构包已通过，仓外前五阶段也通过，但坏资源阶段的**测试夹具**错误期待较晚的 `validate speech product` 诊断。实际能力加载器更早检查产品摘要，因此此前出口为 1、状态不变，但整体仍如实记录为未通过。修正后只接受精确 `load speech capability: product evidence hash mismatch`，并拒绝未知诊断、其他退出码、超时与取消；28 个命名夹具节点通过。产品 Runtime 代码没有为让测试通过而修改。
3. 原未通过根 `speech-product-test-20260905-2210-a1` 和 22:13 外层失败 summary 均保留。最终结果来自重新准入、重新构建及新的 `speech-product-test-20260905-2220-a2`，没有改判原结果。

四项不同的真实符号链接权限 SKIP：`TestSpeechPackageRejectsSymlinkPayload`、`TestSpeechProductExportRejectsIndirectSources`、`TestLocalSpeechContractRejectsIndirectResources`、`TestSpeechManifestRejectsOutsideAndIndirectPaths/symlink`。未更改系统权限，未计通过；其他路径／间接引用负例通过不能替代这些实际 OS 分支。

## 保护、交接与下一步

最终外层 summary SHA-256：`d0873760c581807dd62ca260a919b1ffd35dbe3cb82f5c456932c79233237f35`。仓外正常流程 summary SHA-256：`516e8db6719cbb9a356ba51e29229dacc1acc3bf1dcc3359e2744fe185fd3dec`。完成后再次逐项确认当前 576 项准入来源和 760 项正常构包来源匹配。

日用 local.12 的 74 文件、Runtime PID 31596、Broker PID 31672、开机身份、12 项独立 StdRegProv 注册摘要均保持；builder 的 19 项注册保护也保持，日志只保存摘要。六项历史 x86／ARM64 载荷及原清单只读静态校验保持。没有读取用户文字、真实编码、学习、设置或私人观察文档，没有改变默认输入法、安装产品或运行生产 Rime/PIME／历史冻结二进制。未观测到新的安装版回归。

下一步按[双产品计划](YIME_DUAL_PRODUCT_DEVELOPMENT_PLAN_2026-09-05.md)补齐独立维护门槛，再交接获准的安装／宿主验收：

- 维护配置覆盖、定向退出、完整注册／SID 和失败回退事务仍须验证。源码中既有 `learning.json` 不在 `Get-YimeCoreDataRecords` 顶层白名单，本轮没有修复或声称其恢复安全；进入完整恢复验收前应单独补合同与验证。
- 经明确授权后，才从非打包、同 SID 的原生 Windows PowerShell 进行备份／升级／恢复／回退，保护另一版及默认输入法。此处不自动运行安装命令。
- 之后覆盖受影响 x64／WOW64 x86 注册和真实宿主、显式语流设置、必要重启与隐私安全日用；不读取实际输入内容。
- 物理移除能力、回滚到旧二进制、完整 SR4、L5 用户最终确认、L6、DP1 全部完成、DP2／DP3 共存入口及 ARM64 原生实机均不得从本轮推定通过。

当前无须用户重启或重复手测。两个输入法仍按“可以同时安装，各自独立，互不依赖”的契约推进。
