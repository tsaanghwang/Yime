# exit 51：先补证，再处理现有事务

> **已被单次 Resume 失败复核取代：原恢复方案已执行并再次返回 51，禁止重复执行本页历史命令。当前只交回现有证据，见 [开发端复核与补证交接](YIME_RIME_PIME_EXIT51_RESUME_REVIEW_2026-09-11.md)。**

2026-09-11 已收到 `b2e21d9d3` 的补证；下一阶段见 [原事务恢复方案（待执行确认）](YIME_RIME_PIME_EXIT51_RECOVERY_PLAN_2026-09-11.md)。本页保留补证时的范围；方案尚未执行，不能自行恢复或重新安装。

影响产品：Rime/PIME 候选；现行 YimeCore 为保护对象。

## 当前判定

已接收测试分支 `02297f825`，原报告保留不变。安装 prepared 存在、安装 commit/terminal 缺失；移除 prepared/commit 存在、terminal 缺失。判定为安装失败、回滚完成未证实，不能判为干净环境。36 个注册节点观察为缺失，不等于 Run 值、进程、文件或默认输入状态全部恢复。

三份 peer 对比记录报告 unchanged；这是这些观察时点的保护证据，不扩大为完整安装或回滚验收。

## 给“计算机”端 AI 的下一步

仅在原事务 `84ccacac-c115-4c2d-8d7a-47df8e0ea4a0` 上只读检查，保留原路径与原始字节；不要运行 Install、Remove、Resume，不清理目录或注册表，不修改原包、PIN、approval、boundary、prepared 或恢复票据。不要将开发端的新维护模块复制进旧安装/恢复目录。

从资源管理器启动的原生 PS5 环境，经仓库 run_checked.py 入口执行已有只读检查。当前任务不授予任何安装或清理操作。补充报告：

1. 原恢复票据、approval、boundary、安装与移除 journal 的文件名称、长度和 SHA-256；按原代码只读解析决策，记录 prepared/commit/terminal 的关联校验结论。不要在仓库发布授权内容、SID、MachineGuid 或 delegation material。
2. 按原计划逐项核对 Rime/PIME 已列载荷的存在、大小、哈希及可获取的文件身份；出现变化或不可读就记录，不删除。只列已知产品载荷，用户学习/设置仍只记录必要哈希，不发布内容。
3. 使用系统可见的 StdRegProv 读取原计划指定的 Rime/PIME Run/uninstall 项和注册路径；确认返回码与值类型，禁止 provider 错误时回退进程视图。不要枚举并发布所有用户 Run 项。
4. 对原计划指定的 Rime/PIME 进程核对当前 PID、映像路径和启动时间；观察默认输入状态是否与 retained plan 的原始记录一致。均为只读，不启动或终止产品。
5. 列出仍能找到的原生错误输出、运行时日志及时间线；不存在就明确标为缺失，不能通过再次执行安装来补造原始错误。

原始证据留在 `C:\Users\Golde\Yime Rime-PIME Test Archives`，仓库仅提交结论、必要元数据、证据路径与哈希。只读工具报错时保留错误，不尝试修复现场。提交回 `perf/i7-7820x-local`，由开发端审阅后制定绑定原包与原票据的恢复方案。

## 本轮开发端源码改动的边界

维护模块新增唯一命名、CreateNew、Flush(true) 的外部异常文件，保存异常类型、消息、异常详情、脚本栈、阶段及 PID；安装原始失败与回滚失败分别保存，主控和已加载模块的提升 worker catch 也记录。诊断写入失败不会替换原始异常，不记录完整请求对象或提升 token。

异常位于执行用户的 `Yime Rime-PIME Test Archives/diagnostics`。模块导入前的错误仍只能走原 stderr；本改动不宣称覆盖 PowerShell 无法启动、模块无法导入或断电场景。隐藏提升进程的真实持久化仍需后续受控验证，当前仅有双 shell 隔离维护测试证据。

源码回归 PS5/PS7 各 15 项及来源合同 77 项通过。没有重制诊断安装包，也没有宣称新包能接管旧票据。现有故障根因尚未确定；本轮只补诊断能力和明确暂停点，旧事务的恢复方案仍待上述实际状态证据。
