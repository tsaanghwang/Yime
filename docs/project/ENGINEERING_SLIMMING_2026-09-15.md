# 双产品工程瘦身（2026-09-15）

涉及产品：YimeCore 的历史实验编排、两套产品的共用开发文档。基准提交：`80888e50`。

## 清理结果

- 删除 11 个 `tools/yimecore/run-e6b*.ps1`：它们按旧身份构建、层层调用或注册旧实验宿主，没有当前构建或 CI 调用方。当前身份使用 `build-local-product.ps1` 和 `run-platform-experiment.ps1`。
- 删除无调用方的 `tools/ci/contract-test-lanes.json`；其中列举的 NSIS、DP1 事务任务已经退役。真实 Rime 分片模块与完整覆盖检查继续保留。
- 删除三个过期测试机指令：`TEST-PC-NEXT-STEPS.md`、`TEST-PC-YIMECORE-BUILD-PROMPT.md`、`TEST-PC-YIMECORE-REBOOT-FOLLOWUP.md`。历史源码可在基准提交查询，原始结果文档保持不变；现行指令统一为 [HANDOFF](../../installer/simple/HANDOFF.md)。
- 中英文首页、用户指南、架构和测试说明统一到简版安装器；删除空命令块、旧 NSIS 分发和过期子模块说明。HTML 开发指南收敛为导航，保留 `Open-Dev-Guide.cmd` 的入口。

共删除 15 个跟踪文件，原文件合计 87,278 字节；主要收益是减少失效入口和重复说明，不是大幅缩减磁盘占用。

## 保留内容

两套运行源码、安装器、词典与规范数据、离线工具、锁定的第三方依赖和许可证保持原样。
原始验收证据与历史载荷不清理，未扫描删除本地构建缓存或 `.tmp` 中其他任务的文件。
四组原生 C++ 测试源码（契约、Broker 桥、composition、注册宿主）均保留；历史 E6-B 编排的删除不代表重新取得任何宿主验收结论。

本轮开始时已有的四个未跟踪旧入口退役文件保留原样，不纳入本轮提交；已有 `80888e50` 语流 JSON BOM 测试修正不改写。

## 本地检查

- 删除项在剩余可执行源码和配置中无引用；修改文档没有新增失效的本地链接。
- 当前产品清单的全部资产和 Go 入口存在；真实源码枚举成功，四组原生测试源码仍在集合中。
- PS7 checked 入口：构建契约通过；平台实验 22 项、开发范围 22 项通过。
- CI 调度与分片覆盖 Python 测试：5 项通过。
- 简版单套/双套调度测试通过，使用合成包，不修改已安装产品。
- `git diff --check` 通过。

本轮没有修改运行逻辑，因此不重编译产品或重复历史安装矩阵；完整 CI 结果以本次提交运行记录为准。
