# SR4-A：语流音变自包含试验包与仓外运行

完成时间：2026-09-05 20:03（MYCOMPUTER，Windows x64）。用户要求按[双产品工作顺序](YIME_DUAL_PRODUCT_DEVELOPMENT_PLAN_2026-09-05.md)继续开发。本轮 **SR4-A 通过**：把已准入的 24 条 Stage5C 封装为默认关闭、不可安装的自包含 Broker 试验包，并完成仓外私有运行。不是完整日用安装包、产品设置 UI、安装维护事务或最终 SR4/L5/L6 通过。

[结构化结果与 15 个精确证据引用](../testing/connected-speech/2026-09-05-sr4-package.json)保存哈希、测试级别、真实进程和保护结果。此次没有修改已安装 local.12、默认输入法、生产 Rime/PIME 或用户数据，也不需要用户现在重启或重复手测。

后续更新（21:23）：[SR4-B1 正常产品源码／显式设置](YIMECORE_SPEECH_SR4B_SOURCE_2026-09-05.md)与 [DP1-B 维护保护](YIME_DUAL_PRODUCT_DP1_B_2026-09-05.md)已完成各自源码回归，正常产品构包和安装仍待后续。本文继续保留 20:03 SR4-A 的原始证据，不把旧试验工具重新标成当前源码构建。

## 本轮实现

- 在现有 `YimeSpeechAdmission` 工具中加入 `package`、`verify-package`、`exercise-package`。不复制长期分叉的产品安装器，也不改变当前 `local-product.json` 的版本和安装契约。
- 新契约 `yimecore-speech-candidate-package-v1` 固定 `windows-amd64`、`installable=false`、`default_enabled=false`、`default_bundle=bundle-off.json`。69 个载荷文件合计 143,512,412 字节，另有唯一包清单：两个新编译工具、六个三模式核心／模块索引、五份收据／开关 JSON、56 个正向来源闭包文件。
- `sources/` 是只读来源核对材料，其中 Python 文件不执行，也不进入任何安装器；包外运行不需要 Python、Go、原仓库或另一输入法。源规则、规范读音、码长政策和学习语义均未扩大或改写。
- 验包拒绝未知／缺少／重复的字段、角色、文件或目录，错误大小／哈希、路径逃逸及重解析点、非 AMD64 PE、不支持的导入形式与 Rime/PIME DLL 导入。关闭状态也校验全部三个模块索引，不把 `Load(off)` 当成完整包审计。
- `exercise-package` 先验包，再仅复制 12 个运行文件到全新且不与包根嵌套的私有根；不带旧学习、旧结果或源码。它自行限制子进程环境并固定工作目录。模块显式启用只发生在此测试副本，不改封存包的默认关闭状态。
- 新 `run-connected-speech-package.ps1` 固定前一轮完整准入的 summary SHA、当前源码集合、工具哈希及安装基线；移位时按清单精确复制，工具环境与子进程状态均在固定仓外试验区，不使用继承 TEMP 选取输出根。

## 实际验证

20:01～20:02 重新运行当前源码的完整准入链，没有复用旧二进制作为构建输入：Python 11 项通过；七个 Go 包共 462 个命名测试节点通过（包括父测试和子测试），0 FAIL、2 SKIP。两项 SKIP 都因真实符号链接创建权限不足，分别属于现有 manifest 负例和新增 package 负例；没有计通过，没有更改系统权限。

PowerShell 编排的 57 项 AST／合成合同分别在 Windows PowerShell 5.1 与 PowerShell 7 通过；它们不是安装或进程验收。57 项结果还在本次包编排中重跑并留下固定日志。

封存包随后复制到 `C:\Users\tsaan\YimeCore Isolated Fixtures\SR4\speech-admission-package-20260905-200337-a4344c320b7b45c8925c51d66d865a76`；从该目录启动包内工具，不传仓库、构建工具或安装路径。全部载荷、包清单以及私有运行副本均经过校验。

| 仓外实际阶段 | 独立 Broker PID | 结果 |
| --- | --- | --- |
| 默认禁用 | 36220 | 三模式各自规范／别名来源检查通过，学习代数 0 |
| 启用并明确选词 | 38688 | 三模式各提交两次，学习代数 6 |
| 新进程重开 | 38324 | 学习代数 6，所学别名保持 |
| 禁用后重开 | 4904 | 模块来源撤除，学习代数仍为 6 |
| 重新启用 | 14248 | 三模式规范与别名路径通过 |
| 无效清单拒绝 | 37896 | 专用退出码 42；未打开学习状态，原状态哈希不变 |
| 恢复有效配置 | 38836 | 再次实际重开，三模式规范与所学别名恢复，代数 6 |

前五阶段每阶段执行 72 个规范路径与 72 个别名／禁用来源检查；源侧准入链也独立完成了同样七阶段。未确认输入、分页与取消不产生提交的原检查继续有效；原 50 ms 引擎请求门限没有放宽。

原始封存包与仓外副本在运行前后都通过同一清单 SHA：`009265b9aca18347ac06184199e09460d1a42b0b9b7af6abc24d491e799875d1`。本轮包编排 summary SHA：`861f1bb2f9188769ffa2edf45c62ced64931d608da17b39e05f1373493e52883`。源码准入 summary SHA：`60ecfff394291eb1ba4aa7dfd95f14064e87eda72fcdc34d85ee77ac90e0bc8d`。

## 保护与判定边界

540 项源码／明确输入前后未变，锁定数据未变。已安装 local.12 的 74 文件、Runtime PID 31596、Broker PID 31672、开机身份及 12 项独立 StdRegProv 注册摘要未变；六个历史 x86/ARM64 载荷和原清单只做静态哈希保护，没有执行。所有修改过的进程环境变量精确恢复。

收尾复核：15 个证据文件的大小／SHA、540 项当前源码和 DP1 的 22 个来源再次匹配；试验 Broker 无残留进程。七份相关文档的 51 个本地链接有效，`git diff --check` 及本轮新增文件空白检查通过。独立只读复核未发现阻断本轮 SR4-A 窄范围结论的具体缺陷。

未读取用户正文、真实编码、候选、学习库、私人观察文件或当前用户设置；运行状态完全来自测试新建的私有模型。未观测到新的安装版产品回归。审查中补的包边界与测试编排问题不标为用户日用故障。

仓外运行说明此试验包没有已知源码路径运行前提，但不等于在未安装 Rime/PIME 的干净机器上完成了“仅装自研版”验证，更不代表 x86、ARM64、TSF／真实宿主、Windows 登录重启或日用安装验收通过。源码中的独立试验包不是用户恢复介质，仓外副本与测试状态保留待后续有范围的清理，不自动删除。

## 双产品并行进度与下一步

[DP1 首批源码基线与隔离合同](YIME_DUAL_PRODUCT_DP1_2026-09-05.md)同步完成：22 个固定来源、19 个纯内存测试通过。该合同尚未接入真实安装器；三项既有维护所有权边界已具体登记，不能冒称共存维护通过。

下一工作顺序：

1. SR4-B：把同一准入收据／索引边界接入自研产品的包描述、正常 Runtime/Broker 配置及显式启停设置；验证禁用、坏配置拒绝、用户词库／屏蔽／注释与原功能兼容，以及包内维护回退。不能直接拿本轮 fixture 专用命名空间和 stdio 分支服务日用输入。
2. DP1 后续：先为已识别的 PIME 停进程、旧根清理分支建立具体回归，再接入精确产品所有权校验；不执行生产维护事务。之后才进入获准隔离环境下的 DP2 单装／双向共存维护矩阵。
3. 形成真正可安装候选后另行交接用户安装，并复验受影响注册／真实宿主与必要重启；L5 最终日用确认、L6、签名／公开发行仍分别判定。当前不建立三选一安装器，不改默认输入法。

脚本入口见 [tools/yimecore](../../tools/yimecore/README.md)。本轮属于开发验证，不创建后台日用监控或自动维护任务。
