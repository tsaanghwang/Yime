# 联合补证复核：复现 NSIS 截断恢复摘要

> 修复 CI 已通过，后续交付见[原事务短路径恢复与新候选交付](YIME_RIME_PIME_SHORT_TRANSPORT_RECOVERY_2026-09-11.md)。旧包保持原字节，新包不能接管旧票据。

影响产品：Rime/PIME 候选启动入口。已合入测试提交 `f243eaa07`。十六项只读检查通过；PowerShell 4100 / RecordId 5758 在原失败窗口记录 `Expected literal candidate SHA256.`。所查询的安全日志没有相关阻止事件，当前不应关闭防火墙或安全保护。

已发布 JSON 的字节哈希均与索引一致。仅 `.gitattributes` 文件实测为 `69b41196a7c03d457e057c82b33417fb7168b489cb013f31b06c80d2a3083897`，与索引不符；保留原文件及原索引，不把它写成全部文件校验通过。这不改变 JSON 本身的哈希核验结果。

## 开发端复现与结论边界

用原 NSIS ExecWait 字符串、报告中的测试仓库/归档/临时解包路径及等长合成摘要，构造只展开命令、不启动任何维护的 NSIS 夹具。本机编译执行后，期望命令长度为 1068，实际字符串为 1023；最后的 PreparedSha256 从 64 字符截成 20 字符。所有路径长度与本次已知路径一致，摘要内容使用合成值。

这证明现有安装器存在确定的长命令截断缺陷，且能产生测试日志中的摘要格式错误。现场事件尚未公开完整 HostApplication/参数，故仍区分“本机复现的确定缺陷”和“现场参数截断的直接证据”。最初 Install 失败的内部原因也不能据此一并解释；本轮定位的是 Resume 启动链缺陷。

## 源码修复与验证

NSIS 不再把所有参数拼成一条长 PowerShell 命令，改为在临时解包目录按字段写 UTF-16LE 请求文件，只传请求路径。新增入口按固定八字段解析，验证模式、摘要、字段数量及路径歧义，以参数表传给原维护控制器；不执行参数字符串。原授权、收据、产品边界、目标用户和事务检查继续由原控制器执行。

回归使用真实 NSIS 编译器执行只写请求的夹具，验证长路径及完整摘要不截断；原生 PS5 入口夹具覆盖长路径、空 Install 摘要、截断摘要/额外字段/换行拒绝和子入口失败传递。77 项 baseline 通过。参数传递回归已加入 CI；没有执行本机产品维护。

## 给测试端的当前安排

停止重复十六项诊断，保存原包和所有事务证据。本次只提交源码修复，**尚未交付新候选包，也不授权旧事务 Resume**。不要把新脚本复制进固定旧包，不重新安装覆盖现场。

若现存 RecordId 5758 的原始 XML/消息包含 HostApplication、CommandLine 或参数记录，可仅从已有事件脱敏摘录：PreparedSha256 的字符数、是否等于票据摘要、命令总字符数及调用脚本名，不公开摘要值/授权内容；若没有这些字段就注明缺失，不为补证重跑。

后续恢复需要解决旧包入口截断，同时继续绑定原始包、收据和事务。新安装候选的修复不自动赋予接管旧事务的资格；须另外制作并验证具体恢复交付，再通知测试端执行。CI 成功也不解除本页暂停条件。
