# 当前 local.13 语流能力验证与安装版交接

影响产品：YimeCore。本文件保留 2026-09-10 的 local.13 语流记录，并补充 2026-09-15 测试工具 BOM 修复的已验证结果；本轮没有读取或改变实际用户的语流开关。

## 2026-09-10 历史记录

以下包状态、七阶段结果及文末用户反馈保留自原记录；2026-09-15 的 BOM 修复收尾没有重新核验安装版、运行七阶段进程测试或重做物理输入验收，不能据此推定当前安装状态。

对与安装版完全相同清单的构建包运行正常 Runtime/Broker 七阶段私有进程测试。包版本为 `0.1.0-local.13`，清单 SHA-256 为 `f8dea89ddc04c49af733d99b6dab23afad92857287150f42d6404f458339649b`。使用新建私有目录、独立状态和独立管道，未连接日用 Broker。

| 阶段 | 结果 |
| --- | --- |
| 默认关闭 | 三模式通过，学习代数 0 |
| 启用与合成学习 | 三模式通过，学习代数 6 |
| 启用后重开进程 | 三模式通过，学习代数 6 |
| 关闭 | 三模式通过，学习保留 |
| 重新启用 | 三模式通过 |
| 损坏资源代际 | 按预期拒绝，状态不变 |
| 恢复有效资源代际 | 三模式通过 |

六个正常阶段各检查 72 次规范路线及 72 次别名/关闭行为；不是 72 条不同规则。当前资源为 24 条已审定 Stage5C 上声别名，不代表儿化、轻声及其他语流模块已全部进入 YimeCore。源包与私有包完整性保持。

新增回归修复测试工具的 UTF-8 BOM 兼容：PowerShell 5.1 生成的包 JSON 带 BOM，旧工具在创建结果目录前失败。现在仅在解析时移除开头 BOM，原始文件哈希和大小限制保持，多个 JSON 值仍拒绝；预检失败也输出实际原因。此前失败未启动测试 Runtime，没有可供改判的七阶段结果。

历史验证记录列有 Go 测试和测试工具包回归；本次可确认的重跑命令、结果和范围见下一节。修改只涉及不随产品交付的测试工具和本文档。

原始七阶段证据：`C:\Users\tsaan\YimeCore Isolated Fixtures\SR4B2\speech-product-test-20260910-current-f8dea89d\summary.json`。私有进程重开不是 Windows 重启，也不是安装版设置 UI、注册宿主或物理输入验收；日用设置、学习正文均未读取。

## 2026-09-15 BOM 修复收尾：本次已验证

影响产品：YimeCore 的 `go-backend/cmd/yimecore-speech-product-test` 测试工具。只在 JSON 解析入口剥离一个开头 UTF-8 BOM；保留原始字节大小限制、原始文件 SHA-256 校验和失败原因输出。

| 本次命令（仓库根目录，Go 命令在 `go-backend`） | 已验证结果 |
| --- | --- |
| `go test ./cmd/yimecore-speech-product-test ./input_methods/yime/speechruntime -count=1` | 两个包通过 |
| `go test ./cmd/settings-tool -run Speech -count=1` | 通过 |
| `python tools/powershell/run_checked.py --script tools/yimecore/test-local-product-speech-build.ps1 --edition ps5 --params-file .tmp/speech-bom-regression-params-20260915.json` | PowerShell `5.1.26100.9444`，21 项通过，0 项失败 |
| `git diff --check` | 通过 |

Go 回归覆盖带/不带 BOM 的 JSON、包含 BOM 字节的精确大小边界、多个 JSON 值拒绝，以及带 BOM 清单必须使用原始含 BOM 字节的哈希；解析不改写清单。包工具回归只使用合成文件和源码契约，结果确认 `installed_or_user_data_read=false`、`registry_read_or_written=false`、`package_exported=false`。

本地结果文件为 `.tmp/yimecore-experiment/speech-build-contract-bom-20260915/result.json`，SHA-256：`5b1aa5baa0a582a034e41e2479e48b8d6cfbee9e61c17d2c6186f58cca905802`。参数文件的 `OutputRoot` 为 `C:\dev\Yime\.tmp\yimecore-experiment\speech-build-contract-bom-20260915`；重跑须指定新的输出目录。本节在 Git 中记录已核验结果，本地临时文件不是交接指令或包交付。

本次没有安装、启动或重启产品进程、修改用户设置或改动 Runtime/Broker 载荷；没有生成新产品包，也没有新增七阶段、注册宿主、Windows 重启或物理输入验收结果。

## 历史安装版物理输入步骤（本次不执行）

从当前“音元拼音”的设置打开语流功能，不从历史“自研栈试验版”或 Rime/PIME 设置进入。通过原生设置界面操作，避免 Codex packaged-app 状态视图。先记住原开关值。

1. 将“语流音变”选择为“启用”，点击独立“应用语流”按钮；关闭并重新打开设置，确认启用状态保留，且没有“资源不匹配，未生效”的提示。
2. 在实际应用中选择当前“音元拼音”，对三种编码模式分别手动输入下面两条测试编码，确认出现预期候选。候选可能不在首位；需要选择时使用 Shift+数字，裸数字仍是编码。为减少测试学习，观察后用 Escape 取消即可。
3. 切换焦点时确认未确认组合被取消；关闭语流后规范路线仍可输入，重新启用后别名路线可输入。关闭后已学习别名仍可能出现，不能仅以它出现就断言关闭无效。
4. 报告各模式的布尔结果、设置重开结果和任何错误提示，无需提供个人输入内容。最终开关按用户希望保留的值设置。

以下编码直接取自当前包 `speech/admitted-records.json`，没有手工推导或改动编码源。反斜杠也是一个按键，反引号不是输入内容。

| 词 | 模式 | 规范编码 | 语流别名编码 |
| --- | --- | --- | --- |
| 领导 | full | `\lw/]sso` | `\le;]sso` |
| 领导 | variable、shorthand | `\lw/]so` | `\le;]so` |
| 影响 | 三模式 | `ylw/1ls/` | `yle;1ls/` |

## 用户物理输入反馈（2026-09-10）

用户反馈已接入部分的“领导”“影响”两词在三个宿主中完成测试，并提供了两词各重复六次的输入结果。本次将两词三宿主人工输入记为用户报告通过，不再列为未执行。

本条反馈未列出三个宿主的名称、位数、各次使用的编码模式或规范/别名路线，因此不补写这些细节，也不将输出次数推定为每个宿主的三模式覆盖。它与上面的三模式七阶段隔离证据分别成立。设置关闭重开、焦点取消及关闭/重新启用等步骤没有得到逐项确认，仍保留未确认状态；不因这条反馈要求用户重复已经完成的两词三宿主测试。

安装版 x64/x86 专门语流注册宿主测试、真实维护恢复和更广规则准入仍分别待办，不能由上述隔离及人工样例通过提前关闭。此次无需为测试工具修复重新安装产品，也未要求立即重启 Windows。
