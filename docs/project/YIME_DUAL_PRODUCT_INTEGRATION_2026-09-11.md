# 双产品开发分支统版记录

影响产品：YimeCore 测试证据、双产品后续制包计划及共享 CI。合入 `perf/i7-7820x-local` 的 `1f07836b`，保留测试分支历史。此前以独立提交接入的非法码元拒绝源码与宿主采集器由 Git 合并对齐，不重复替换。

## 当前统一状态

- 测试机“计算机”：已安装 YimeCore local.13，manifest `133c91fe205fcae911fa701a278465dad040e7dadce006b541697b656e50405a`，安装元数据源码 `e346a1dad76d58f50652a2dd665c64ec8aeef49e`。不等同于开发分支最新源码或 MYCOMPUTER 的 `f8dea89d…` 安装包。
- 四宿主三模式、每模式三轮：人工报告通过，模块身份有独立采集记录；26 份导出 JSON 原始哈希已核对。Firefox 的部分进程未加载 TIP 的原始失败字段保留，按实际加载 TIP 的进程和人工结果联合判断。
- 重启登录：本次独立系统注册表、同用户当前 Runtime/Broker 及 Shell-Core 9708 证据已补齐。历史默认输入法/注册前后保护对照仍暂缺，不回填或倒推。
- E3：六份原始基准文件哈希核对通过，但旧包 learned/static p95 开销门槛仍失败。其 manifest 为 `086172b2…`，不是上述安装包。原始性能失败结果不改变。
- 非法码元拒绝及索引状态文件占用修复：属于更新的源码；此次合并不安装新包，不能标成测试机已验证。
- CI 六任务并发上限为 6，保留每任务独立 Windows runner、双 shell 覆盖、fail-fast 和所有前置/汇总检查；实际并发取决于 runner 可用性。

## 安装工作分工

用户确认“计算机”从未安装 PIME/Rime。它已装 YimeCore，因此作为“YimeCore → Rime/PIME”的共存测试目标规划；该用户说明不能代替安装前实际注册/文件探针。

主开发端先实现并验证已有 peer 的限定准入及维护保护，提供固定候选、哈希、操作入口和交接说明。测试端按交接执行，回传原始证据，失败保留现场。当前 09-09 Rime/PIME 受限候选仍要求干净独立 x64 目标，不能直接用于这台共存环境；单产品实际验收也不能用共存结果替代。

本轮为分支整合及文档一致性整理，未执行安装、卸载、升级、注册或用户数据迁移。四宿主行为验收保留有效，DP1-U 全部实际维护、DP2 共存矩阵、DP3 三选一入口及公开发布仍未完成。

统版验证：暂存区及工作区的 26 份宿主记录、6 份基准记录均匹配原索引哈希；新增只读采集器 PS5/PS7 合同通过；双产品来源合同 77 项通过。`7a14290e` 的 CI 34494287337 已完整成功；此处不把该成功继承为本次合并提交的 CI 结果。
