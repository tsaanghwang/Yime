# Rime dependency schema
# Compiles the explicit, source-backed variable-mode erhua mixed overlay.

schema:
  schema_id: yime_erhua_mixed_variable
  name: "Yime 显式儿化混合表（变长依赖）"
  version: "explicit-erhua-mixed-v4"

translator:
  dictionary: yime_erhua_mixed_variable
  enable_user_dict: false
  enable_sentence: false
  enable_completion: true
