# i7-7820X 测试机：YimeCore 本机构包指令

请在 i7-7820X 测试机上的 Codex 中执行以下任务。

为这台测试机制作用于本机验证的 YimeCore 开发安装包。影响产品仅为 YimeCore；本次授权检查环境、修改必要源码、构建和隔离测试，只制作安装包及安装说明，暂不安装。

1. 先读取项目的 `AGENTS.md`、`tools/yimecore/development-scope.json` 和 `docs/project/YIME_DUAL_PRODUCT_DEVELOPMENT_PLAN_2026-09-05.md`。确认当前执行环境确实是测试机，记录实际计算机名、Windows 版本与位数、CPU、内存、构建依赖和 Git 分支状态。若不是受支持的 64 位 Windows 环境，先报告具体差距。
2. 确认本地开发分支包含提交 `e346a1dad`（YimeCore 安装交接和回退修复）。若缺失，先检查远端及本地改动，再安全整合所需提交；保留用户未提交的工作，不覆盖或重置它们。
3. 若项目目录通过共享访问，先确认它是否与另一台机器正在使用的工作目录是同一份可写检出。不要让两台机器共用 Git 索引或构建输出；必要时在测试机本地建立独立检出，用共享文件传递指令。构建产物写入测试机本地独立输出。
4. 按已批准的 mainstream Windows x86-64 实体测试机范围，使用 `tools/yimecore/run-platform-experiment.ps1` 的实验通道。先检查该通道已实现的能力；若尚不能制作本机安装包，补齐范围明确的测试机专用构包入口。保留 MYCOMPUTER 本地产品通道的限制，不要取消机器校验、把所有机器加入默认通道，或把已有 MYCOMPUTER 包改名当作新包。
5. 使用当前源码和当前有效 YimeCore 产品身份，构建 x64 Runtime/Broker 及 x64/x86 TSF。x86 仅代表 64 位 Windows 下的 32 位应用兼容面。不得执行或重新标记历史身份的 x86/ARM64 载荷，不扩展到 32 位 Windows 或未批准目标。
6. 检查并准备所需构建依赖，遵守仓库的数据来源边界和工具链约束。不得从其他 Git 仓库导入词库或读取另一台机器的用户学习数据；缺少授权的数据源时报告具体缺口。
7. 完成与改动相关的隔离合同测试、独立运行测试和包完整性验证。记录源码提交及未提交补丁、产品版本、包清单 SHA-256、目标计算机及架构，确保安装包自带所需生成资产和维护依赖，不依赖另一产品的已安装文件。
8. 输出安装包的绝对路径、面向这台机器的安装说明、恢复/回退前提和安装后验收步骤。分别列明源码构建、隔离运行、静态完整性哪些已通过，安装、注册宿主、真实应用输入及重启登录自启动哪些尚未执行。交叉构建不能当作原生运行证明。

本次不要安装、卸载、注册输入法、修改默认输入法、停止现有输入法进程，或读取/迁移/修改用户设置和学习数据。不要执行历史载荷，不购买硬件、不创建云机器。后续实际安装另行交接；若原生维护要求 Explorer 启动的普通 Windows PowerShell，应准备具体命令，不绕过 packaged-app 祖先进程保护。

最终用中文报告结果和可直接使用的文件路径。不要将此次构包记为真实宿主验收或正式发布完成。
