[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[project]
name = "yime-lexicon-toolchain"
version = "0.1.0"
description = "Offline canonical pinyin, lexicon, layout experiment, and acceptance tools for Yime."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=9.0"]

[tool.setuptools]
packages = []
