# 给开发者 AI：连续编码非法码元处理

测试机“计算机”在 YimeCore local.13 的 Notepad++ x86 三模式真实宿主测试中发现：连续编码加入一个不符合当前编码规则的码元后，现有实现会把整个非法串留在预编辑中，要求用户再按退格修正。

用户要求改为局部拒绝：第二码及以后出现非法码元时，只取消本次错误码元，保留此前合法组合、候选和继续输入能力；首码不在当前模式编码范围时也应吞掉该错误码元，不创建预编辑。裸数字属于合法编码键的既有契约不变，`Shift+1` 至 `Shift+9` 仍用于候选选择。

本分支实现采用确定性行为：先让 Broker 判断实际结果；若按键被当前 schema 忽略，或追加后得到“无候选且无句子”的非法状态，则只回退本次追加，再把恢复后的状态交给 TSF。提示音可作为后续可选增强，但不能成为正确性依赖，因为静音配置、无声卡、远程会话和系统声音策略都可能使其不可闻。

回归必须覆盖：合法前缀后的非法码元、首码非法、回退后 Backspace 仍删除最后一个合法码元、继续合法输入可恢复原组合，以及 x64/x86 注册宿主均不把非法码元泄漏到宿主正文。

开发分支新增的 `tools/powershell/run_checked.py` 与本改动没有文件或行为冲突；后续 PowerShell 脚本调用应统一经过该入口。该入口按其契约不检查完整外部依赖，因此 `cmake` 已安装但未进入当前 PATH 时仍需由工具链初始化或明确路径解析处理，不能把预检通过解释为构建环境已就绪。
