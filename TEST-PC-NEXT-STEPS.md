# 给测试机“计算机”的 AI：后续任务交接

用户要求两台机器并行推进，并要求将本说明推送到仓库供测试机 AI 读取。本文适用于开发者所有的 i7-7820X、Windows x64 测试机“计算机”。先读取 AGENTS.md；保持现有安装和数据边界。

## 当前已知结果

- 测试分支 `perf/i7-7820x-local` 的 `fc2d7db65` 已在主开发分支整合；主开发分支 `codex/yimecore-replacement-experiment` 的 `57ea36bde` 补了 PS5 中文机器名测试编码兼容和报告交接。
- `d4464c3d4` 报告当前 YimeCore local.13 安装、x64/x86 注册宿主及重启后基本输入通过。安装清单为 `133c91fe205fcae911fa701a278465dad040e7dadce006b541697b656e50405a`，不能替换为 MYCOMPUTER 的 `f8dea89d…`。
- `1b03cf76b` 发布了 2026-09-09 的 E3 基准和 CPU 摘要。测量源码为 `222f2d5f6411df1a942b12d0a11735a7205fe7df`，基准包清单为 `086172b22c94933b727e8afebd156b29a581312bbeb61e5b78047518a1c1d842`。这是独立基准包，不是当前安装包。
- 两轮 E1/E2 全通过；E3 正确性全通过，但 p95 learned/static 比率多数超过 1.10，完整性能门禁未通过；p99 均未超过 1.20。约 54 秒的登录启动间隔是另一问题，不与 E3 混为一谈。

## 现在请执行

1. 确认当前确为“计算机”和测试机本地独立检出。获取远端更新，但不要覆盖本地改动，也不要为了取得本文而盲目合并整个主开发分支。可直接读取 `origin/codex/yimecore-replacement-experiment:TEST-PC-NEXT-STEPS.md`；如需合并修复，先查看具体差异。
2. 按 `TEST-PC-YIMECORE-REBOOT-FOLLOWUP.md` 补当前已发生的重启登录的只读证据：独立系统 Run/卸载记录、包哈希、当前 Runtime/Broker PID/路径/父子/SID/开机时间，以及匹配的 Shell-Core 9708。不要为制造通过而重启系统、手动启动程序或修改日志设置。若当前进程已手动重开、事件缺失或无法取得可信观察，记录限制即可。
3. 整理用户已经给出的实际输入反馈，注明具体宿主及位数、编码模式和测试项目；没有给出的信息保留未知。不得把入口存在推定为所有应用兼容，也不要要求用户重复已确认的测试。允许记录用户后续主动反馈的可复现异常，不自动抓取输入内容或学习数据。
4. 整理 E3 证据换行：`docs/testing/yimecore/native-core/2026-09-09-i7-7820x/SHA256SUMS` 的六个摘要对应原始 CRLF 文件，而 Git blob 为 LF。主开发机已确认六份均在还原 CRLF 后匹配。优先对这些明确证据路径设置适当 Git 属性并保留原始 CRLF 字节，使检出后及 Git blob 的原始字节哈希可核对；不要无说明地重算摘要覆盖旧摘要或改写失败结果。更改前后分别记录原始摘要、Git blob 摘要及换行原因。
5. 将新增的脱敏结果 JSON、说明和必要的证据换行修复提交、推送到 `perf/i7-7820x-local`，报告提交号和未完成项。原始大日志、pprof、程序和合成学习状态继续留在本机，不上传用户数据。

## 暂时不要执行

- 不重复运行旧 E3 包，不修改门限、学习逻辑或测试迭代数来制造通过；不要另外启动一条与主开发机冲突的性能优化分支。
- 不重新安装当前 YimeCore，不主动做升级、卸载、恢复或清理历史文件。
- 不安装 Rime/PIME 候选，不移除其 clean-target/peer 注册检查，不改变默认输入法。
- 不运行有实际会话或状态副作用的 `Maintain Verify` 来代替只读重启采集，不绕过 Explorer 原生维护保护。
- 不创建云机器、不购买硬件、不自动设置定时任务或持续监控。

## 收到新性能候选后的工作

主开发机负责继续双产品制包和维护边界工程，并依据 CPU 证据定位 E3 开销。只有收到明确的新基准包、源码身份、清单哈希及对照指令后，再执行新一轮测试；不要把本说明当作未来任意程序的执行授权。

届时保留旧包和原始失败结果，在同一测试机和可比条件下运行旧/新包：同一电源计划、相同迭代及预热设置、相同合成工作负载和隔离状态规则。每轮都记录包身份、参数、正确性、p95/p99 比率及实际耗时（工具有输出时），注明机器负载和顺序。旧基线的 1000/100 次迭代仅作为已知记录，具体复测参数以新交接为准。

CPU 采样比例不是端到端输入延迟。新的基准通过也不能自动替代安装版、TSF、真实应用或完整双产品维护验收。

完成当前只读任务后报告结果即可，无需持续等待或重复轮询；后续用户会提供新包交接。
